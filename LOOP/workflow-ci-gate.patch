diff --git a/.github/workflows/R-CMD-check.yaml b/.github/workflows/R-CMD-check.yaml
index c895e26..4f554c3 100644
--- a/.github/workflows/R-CMD-check.yaml
+++ b/.github/workflows/R-CMD-check.yaml
@@ -33,6 +33,12 @@ jobs:
     env:
       GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
       R_KEEP_PKG_SOURCE: yes
+      # Run the vignette code-tangling step. It defaults to skipped whenever check
+      # re-builds vignettes, which is why six vignettes silently executed engine
+      # code under `R CMD check` for months while CI stayed green -- CI structurally
+      # could not see it. Setting this false is what makes that class of regression
+      # catchable here rather than only in a local `devtools::check()`.
+      _R_CHECK_VIGNETTES_SKIP_RUN_MAYBE_: false
 
     steps:
       - uses: actions/checkout@v5
