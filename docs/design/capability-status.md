# drmSEM capability surface

**Current development version:** 0.5.0 (`DESCRIPTION`); `NEWS.md` carries additional unreleased "maturity hardening" work under a `(development version)` heading not yet version-bumped.
**Lifecycle:** experimental (README badge; `docs/design/05-roadmap.md` still lists 0.4's engine-dependent items as open).
**Scope:** this file is a capability inventory of what `drmSEM` exports and what evidence backs it — it is not a release certificate, not a claim that every path, effect, or model comparison is scientifically correct for a given dataset, and not a substitute for `NEWS.md`, `docs/memory/OPEN_QUESTIONS.md`, or `docs/memory/VALIDATION_LEDGER.md`. When this file and those disagree, re-derive from `NAMESPACE`, `R/`, and `tests/testthat/` — those are the ledger; this file is a projection of it. Evidence below includes a fresh full local test run (**2026-08-28**, `drmTMB` **0.7.0** installed and fitting: **1213 passing / 0 failing / 5 skips / 10 warnings**) — the strongest evidence this file cites, since several repo docs (e.g. `docs/design/04-validation-plan.md`) predate that and describe the drmTMB-integration tests as unrunnable in a dev container. The two extra skips versus the historical three are V-127 / V-127b (`test-imputation.R`): this machine's installed `drmTMB` 0.7.0 predates engine #1097 (`has_mi` for ZIP); those tests passed on the #56 consumer-lift lane against that engine. The other three skips remain the unreachable `ape`-missing guard and 2 `symbolizer`-not-installed tests.

## What the package is for

`drmSEM` is a **distributional piecewise structural equation modelling (SEM) layer built on the `drmTMB` fitting engine**. `drmSEM` never fits its own likelihoods: every endogenous node is exactly one `drmTMB` fit, and the SEM is piecewise (a graph of separately fitted local models, in the tradition of `piecewiseSEM`). Its distinguishing feature is that a causal path does not have to target the mean (`mu`) — it can target any modelled distributional component of a node: residual scale (`sigma`), shape (`nu`), zero-inflation (`zi`), hurdle probability (`hu`), random-effect scale (`sd(group)`), or bivariate residual correlation (`rho12`). On top of that graph, `drmSEM` provides component-labelled path tables, an any-component d-separation test with Fisher's C, and Monte-Carlo (never coefficient-product) direct/indirect/total effects that separate a mean-mediated channel from a distribution-mediated (Jensen-gap) channel.

## Capability matrix

| Capability | Status | User-facing entry point | Evidence / boundary |
|---|---|---|---|
| Piecewise DAG assembly (declarative and pre-fitted interfaces) | covered | `drm_node()`, `drm_sem()`, `drm_psem()` | `drm_sem()` (fit-then-delegate) is the one exercised throughout: `tests/testthat/test-integration.R`, `test-phylo*.R`, `test-recovery-*.R`, `test-composite.R`, `test-feedback.R`, `test-pair.R`, `test-model-set.R`, `test-symbolize.R` all build real `drm_sem()` graphs against a live `drmTMB` fit (confirmed by a fresh full local run, **2026-08-28**, `drmTMB` **0.7.0**, `NOT_CRAN=true`: **1120 assertions pass across 38 test files**, 5 skips — see the header). Cycle rejection (undeclared cycles are a hard error) is asserted in `test-feedback.R`. **`drm_psem()` covered as of 2026-08-15** by `tests/testthat/test-psem.R` (4 tests / 20 assertions, V-105..V-107): it assembles pre-fitted nodes into an object whose graph, path table and *coefficients* match a `drm_sem()` built from the same data; the downstream surface (`dsep()`, `fisher_c()`, `indirect_effects()`, `check_sem()`) works on it; non-`drmTMB` input is rejected with an error naming `drm_sem()`; and the previously untested default-`data` branch (deriving the frame from the first fit) is asserted to agree with the explicit-data form. Also pinned: a **saturated** DAG yields an empty basis set and says so — correct behaviour that reads as breakage. |
| Component-labelled path table | covered | `paths()` | `test-integration.R` ("`paths()` returns a component-labelled coefficient table") and `test-phylo*.R`, `test-recovery-*.R` assert a `zi`/`sigma`/`mu`-labelled table on live fits; `test-phylo-distributional.R` specifically confirms a `sigma` edge is exposed and the `phylo()` marker never leaks into `from`. |
| Any-component d-separation and Fisher's C goodness-of-fit | covered | `basis_set()`, `dsep()`, `fisher_c()` | Graph logic kernel-tested in `test-dsep-kernels.R`; live detection of a deliberately omitted true edge in `test-integration.R`; phylo-node augment-refit (OQ-13) in `test-phylo.R`. **Scale-aware C membership (D-21, V-109c):** a claim whose added variable is constant within a grouping the node does not model is `status = "wrong_scale"` and is excluded from Fisher's C (`test-scale.R`); detection (`n_effective`, `scale_group`) shipped earlier (V-109). This is not a rewrite of the user's node. **Calibration (V-17):** the cached `inst/calibration/calibration-results.rds` (14,400 replicates: 3 DGP families x 4 n x 6 omitted-edge strengths, `drmTMB` 0.1.3.9000) passed all 5 pre-registered acceptance checks (Type-I near nominal by family/n and by claim-df, uniform null Fisher's C, high ordered power) — but only for that grid; broader d-sep/Fisher's C settings are explicitly claim-scoped, not generalized (`docs/memory/OPEN_QUESTIONS.md` OQ-6, `docs/design/12-coverage-calibration.md` C-2, not yet run). `test-calibration.R` is a fast 20-rep CI smoke of the same claim, not the full study. MAG m-separation under `latent =` is the next row — it changes which claims are generated, not the any-component LRT. |
| MAG m-separation under latent confounding (`latent =`) | partial | `drm_sem(latent =)`, `drm_psem(latent =)`, `basis_set()`, `dsep()` | When `latent =` names **marginalised** latents (`L_M`), `basis_set()` / `dsep()` generate and test **m-separation** claims on the implied MAG rather than d-separation on the observed DAG. Conditioning is Richardson & Spirtes (2002) Corollary 5.3 **anteriors** (`ant({X,Y}) \\ {X,Y}`; selection set \(S = \emptyset\)), not Shipley & Douma observed parents; bidirected spouses are not anteriors. Pairwise ⇒ global is licensed by Sadeghi & Lauritzen (2014) Theorem 3 / Lauritzen & Sadeghi (2018) Theorem 4 under a **compositional-graphoid** assumption (automatic for homoscedastic all-Gaussian nodes; otherwise faithfulness, with one `cli_inform()` per `basis_set()` call). The any-component LRT (D-2) is unchanged — only which claims are generated changes. Evidence: V-117 printed Cor. 5.3 claim set + collider trap (`tests/testthat/test-mag-basis.R`); V-118 / V-119 live DGP where DAG d-sep is wrong and MAG m-sep is right (`test-mag-dsep.R`); conversion kernel V-112..V-115b (`test-mag.R`). **Does not cover:** this is a **graph projection** of latent confounding onto a piecewise observed SEM, **not** FIML and **not** a joint latent-variable likelihood; selection / conditioned latents are structurally unrepresentable (no selection argument); parent-based Shipley & Douma separators remain unlicensed (L&S 2018 §5.3 Ex. 1); residual **OQ-16** — drmSEM's any-component LRT / Fisher's C is not shown to *induce* a compositional graphoid. m-separation does not extend the component-labelled path claim: a path to `sigma` is still not a path to the mean. See `docs/design/14-m-separation.md` and D-20. |
| Model / fit diagnostics | experimental | `check_sem()` | Implemented in `R/diagnostics.R` (per-node convergence, `vcov` availability, and whether the family has a realized-value sampler for distribution-mediated effects). **Covered as of 2026-08-15:** `tests/testthat/test-diagnostics.R` (8 tests / 26 assertions, V-97..V-102) asserts the row content against a live SEM — column set, topological row order, `nobs`, `converged`, `vcov_available`, `sampler`, and the `exogenous` attribute — and every warning branch of the `print` method against **hand-built** `drm_diagnostics` objects: non-convergence (including `NA` convergence, which is treated as not-converged rather than as fine), missing covariance (asserting the message names the `se = TRUE` remedy), missing sampler (asserted to `inform`, not `warn`, because mean fallback is a documented degradation), and mismatched `nobs`. The split is deliberate: asking a live optimizer to produce a non-converged node on demand is exactly the test that passes on macOS and fails on Windows. |
| Simulation-based effect decomposition: direct, indirect, total; mean- vs distribution-mediated | covered | `direct_effects()`, `indirect_effects()`, `total_effects()` | The headline mechanism (a mediator's `sigma` responding to the exposure, propagated through a downstream nonlinearity into a real mean shift) is kernel-verified in `test-effect-kernels.R` and asserted end-to-end on a live fit (V-41 in `test-analytic-effects.R`, confirmed passing under `NOT_CRAN=true`). The family x link recovery grid (gaussian/poisson/nbinom2/beta/beta_binomial/Gamma/lognormal; V-45..V-54) and sampler-moment recovery against `drmTMB::simulate()` (V-55..V-64) are in `test-recovery-families.R` / `test-recovery-samplers.R`, both live-fit and passing locally. Effect-CI coverage for the parametric-uncertainty intervals is validated only for a linear-Gaussian chain (cached C-1 grid, `inst/validation/validation-results.rds`, `docs/design/12-coverage-calibration.md`). **Does not cover:** effects are **conditional** (random effects held at zero) — `population = "marginal"` aborts immediately with a not-yet-implemented message (`R/effects_api.R`), so a path into `sd(group)` appears in `paths()` but has no marginal effect-decomposition entry (OQ-9); `uncertainty = "bootstrap"` likewise aborts immediately (OQ-10); realized-value sampling for distribution-mediated propagation covers `gaussian`, `student`, `skew_normal`, `lognormal`, `Gamma`/`gamma`, `poisson`, `nbinom2`, `truncated_nbinom2`, `beta`, the continuous part of `zero_one_beta`, `tweedie`, `binomial` and `beta_binomial` (`drm_supported_sampler_families()` in `R/diagnostics.R`), each with a moment test against `drmTMB::simulate()` (V-55..V-61, V-82..V-85), and V-86 locks that advisory vector to the samplers actually implemented so it cannot go stale again. **`binomial`/`beta_binomial` are conditional on `trials` being recoverable from the fit**; without it they warn and fall back rather than returning a probability where a count is required. Still mean-fallback: `cumulative_logit` (needs an engine decision about a non-numeric mediator value) and the bivariate families. **Hurdle mediators fixed 2026-08-15** (`test-hurdle.R`, V-103/V-104/V-108): previously a hurdle node reported `sampler = TRUE` and was then drawn as a plain truncated NB2, silently dropping its hurdle zeros, because drmTMB folds the hurdle into `model_type` while `family$family` stays `truncated_nbinom2` and drmSEM keyed on the name. `drm_effective_family()` now prefers `model_type` for an explicit allow-list (`drm_model_type_samplers()`), so `hurdle_nbinom2` reaches its own sampler while `zi_poisson`/`zi_nbinom2` keep using the base family plus generic `zi` post-processing — keying on `model_type` wholesale would have sent those to the mean fallback. Verified against `drmTMB::simulate()`: zero fraction 0.4444 vs 0.4446, mean within 0.4%, variance within 2.7%; a feedback (cyclic) SEM is refused by `indirect_effects()` (see the feedback row below). |
| Outcome functionals beyond the mean; natural (cross-world) effect decomposition | partial | `direct_effects(target=)`, `total_effects(target=, functional=)`, `indirect_effects(target=, effect="natural")` | `target = c("mean","p_gt","p_zero","var","quantile")` rides all three effect functions and is kernel-verified (a `sigma`-path quantile shift, `test-effect-kernels.R` V-74/V-75) and live-recovered for Poisson `p_zero`/`var`/`p_gt` (V-62..V-64, `test-recovery-samplers.R`). **Does not cover:** `functional = "analytic"` (closed-form, no Monte-Carlo noise) is implemented and kernel-verified (V-76) for exactly two families, **gaussian and poisson**; every other family aborts with a pointer back to `"simulate"` because their sigma-to-dispersion mapping is still open (OQ-1). `effect = "natural"` (Pearl/Imai cross-world NDE/NIE) is validated only on the identity-link linear-Gaussian recovery case (`test-effect-kernels.R`); general cross-world identification under arbitrary links/interactions, an integration test on a live nonlinear fit, and CIs for the natural split remain open (OQ-8). `effect = "natural"` and a feedback SEM are both mean-only by construction. |
| Per-mediator and per-component path attribution | covered | `path_effects()` | Additive/nonlinear/sequential mediator decomposition (P-1..P-3) and the per-component (`mean_channel`/`sigma_channel`/.../`component_remainder`) split against the lognormal closed form are kernel-verified (`test-path-effects.R`, V-32..V-35), and a live fitted-SEM integration test (Gaussian mediator with `sigma ~ x` feeding a log-link Poisson outcome) confirms both `by = "mediator"` and `by = "component"` return finite `drm_effect` objects (`"path_effects() runs on live fitted SEMs"`, confirmed passing under `NOT_CRAN=true`). Recanting-witness detection for the natural per-mediator variant is kernel-verified (V-35). **Does not cover:** this is model-based attribution, not a nonparametric path-specific identification claim (documented, not a defect); `NA` handling for unconfirmed-sampler families in the natural variant is still an open item (OQ-5). |
| Standardization of path coefficients | covered | `standardize()` | `sd_x` (predictor-SD scaling) and `latent` (also dividing by the component's fitted linear-predictor SD) are both kernel-tested (`test-standardize.R`), including the theoretical link-variance term added to the `latent` mean-path divisor for logit/probit/cloglog (V-44). **Does not cover:** the log-link families' mean-dependent (observation-level) latent variance term is explicitly deferred (OQ-4); a Gelman (2008) 2-SD opt-in for continuous-vs-factor comparability is not implemented. Reports link-scale only by design (no back-transform), and factor predictors keep SD = 1 (documented convention, not a bug). |
| Confirmatory model comparison and selection | covered | `drm_dag()`, `drm_model_set()`, `compare()`, `best()`, `average()` | `test-model-set.R` builds a real candidate set (true / omitted-edge / saturated DAGs), fits it live, and asserts `compare()`'s CBIC ranking, weight normalization, `best()` returning the top `drm_sem`, and `average()` returning CBIC-weighted standardized paths. `CICc` arithmetic and the CBIC-vs-CICc correction formulas are kernel-tested. Model-selection recovery (C-3: does `best()` pick the true DAG?) passed in the cached live-lane run for `CBIC`, which is why `CBIC` (not `CICc`) is the default criterion. **Does not cover:** `CICc` is explicitly scoped as a phylopath-style support measure, not a validated consistent selector — it may retain an over-fitted rival (documented, not a defect); **`average(method = "latent")` covered as of 2026-08-15** by `test-model-set.R` (V-116): `standardize()`'s own latent scaling was already validated (V-44, V-65), but nothing checked that `average()` *forwards* `method` — so a dropped argument would have returned a well-formed `drm_average` carrying the `"sd_x"` numbers under a `"latent"` request. The test rebuilds the CBIC-weighted mean by hand from `attr(cmp, "fits")` with `standardize(fit, method = "latent")` and asserts equality, asserts the two methods actually differ, and pins `weight_sum` and the path-key set as properties of the graph rather than of the standardization. Confirmed to fail when `method` is hard-coded to `"sd_x"` in `average.drm_compare()`. |
| Latent & MIMIC measurement blocks (formative, reflective, MIMIC) | covered | `drm_latent()`, `drm_indicator()`, `drm_composite()`, `loadings()`, `reliability()` | Fixed-weight, PCA, and FA scoring; marker indicator and unit-variance identification; Cronbach's $\alpha$ and Raykov's $\rho$ reliability calculation; standardized loadings; separate `loadings(sem)` from `paths(sem)`; end-to-end MIMIC model parameter recovery (V-130..V-132); intervention propagation on latent nodes $do(\eta) \to z$ and structural causes $do(x) \to \eta \to z$ through $\mu$ and $\sigma$ distributional targets (V-133); MAG m-separation projection without spurious cycles (V-134 in `tests/testthat/test-latents.R`). |
| Residual / higher-level covariance-edge declarations | covered | `covary()`, `covariances()` | Declaration, validation, node resolution, de-duplication, and the accessor's residual-vs-higher-level split are kernel-tested (`test-covariances.R`); `basis_set()`/`dsep()` dropping the `y1 ⊥ y2` independence claim for a declared edge (Shipley's bidirected-edge rule) is asserted both in isolation and via `drm_pair()`'s auto-detected edge (`test-pair.R`). This is a pure-R grammar + d-separation-awareness layer — it declares and reports covariance allowances, never estimates a correlation itself. |
| Bivariate (joint two-response) residual correlation | partial | `drm_pair()`, `drm_sem()`, `drm_psem()`, `rho12()`, `corpairs()` | Declaration, `covary()` bridging, and d-sep dropping `y1 ⊥ y2` remain kernel-tested (`test-pair.R`). **Joint fit shipped (0.4):** `drm_sem()` / `drm_psem()` fit or consume one `biv_gaussian` / `biv_lognormal` / `biv_student` model; both response names share that fit; `rho12()` returns the Wald table of `rho12` coefficients (intercept and `rho12 ~ x`) on the `tanh` link, with SE and p-value. Recovery: V-128 constant residual correlation 0.45 (response-scale |tanh(intercept) − 0.45| < 0.12); V-129 `rho12 ~ x` recovers tanh-link intercept 0.20 and slope 0.50 (`test-rho12.R`). `paths()` reports directed `x -> rho12` once (attached to the first margin). **Does not cover:** this is still one drmTMB bivariate node, not a global multivariate SEM likelihood; mixed-family pairs abort; deep level-compatibility validation against fitted RE blocks is not implemented; higher-level `corpairs()` estimates are read from the engine when present, not estimated inside drmSEM. Status stays `partial` for those leftovers. |
| Cyclic / feedback motifs with equilibrium effects | partial | `drm_cycle()`, `cycles()`, `total_effects()` (equilibrium), `direct_effects()` | Declaration, the relaxed toposort that still rejects any *undeclared* cycle, and multi-component fixed-point propagation with adaptive relaxation are kernel-tested against the closed-form linear reduced form `(I-B)^-1 Gamma` (V-42, V-135), stability boundary detection with spectral radius and empirical Lipschitz contraction diagnostics (V-136), nonlinear multi-component lognormal + ZIP equilibrium recovery (V-137), variance-moderated feedback loop recovery (V-138), and nonlinear 2-cycle self-consistency (V-73, `test-feedback.R`); a live-fit check confirms the equilibrium `total_effects()` matches the reduced form built from the **fitted** B matrix (V-72 in `test-recovery-structural.R`). **Does not cover:** `indirect_effects()` and `path_effects()` both refuse a feedback SEM outright (`expect_error(..., "feedback motif")`, `test-feedback.R`) because the mean/distribution decomposition through a cycle is undefined; node-wise fitting of a declared cycle is **inconsistent under simultaneity** and `drm_sem()` only warns about this — it does not implement IV/2SLS or a joint likelihood to fix it; full sigma-separation is not implemented. |
| Graph interchange with lavaan syntax and Graphviz DOT | covered | `as_lavaan()`, `from_lavaan()`, `as_dot()` | Round-trip recovery of the directed mean structure and covariance edges (`from_lavaan(as_lavaan(sem))`), the honesty rule that every non-`mu` path is dropped-with-notice rather than misrepresented as a lavaan mean regression, and DOT emission keeping every component path are all kernel-tested (`test-interop.R`, pure string/graph logic, no engine needed). **Does not cover:** this is graph interchange only, not a fitting bridge — `from_lavaan()` ignores reflective (`=~`) measurement lines with a warning rather than building a measurement model; no brms/lavaan *fitting* interop exists or is planned for 0.x. |
| Phylogenetic relatedness matrices and phylo-aware SEM | covered | `drm_phylo_cov()` (feeding a node via `relmat()`; `phylo()` itself is a `drmTMB` marker, not a `drmSEM` function) | BM/Pagel's-lambda/OU/Pagel's-kappa matrix transforms are kernel-tested without `ape` where possible and against a real tree otherwise (`test-phylo-cov.R`); correct exclusion of the `phylo()` marker from the causal edge set, d-sep augment-refit resolving the tree in the SEM's captured environment (OQ-13, resolved), and effect propagation through a phylo node are live-fit tested (`test-phylo.R`). The combination the design doc frames as future "Phase 4" work — a phylogenetically-structured mediator that **also** carries a distributional (`sigma`) path — is in fact already live-fit tested end-to-end and passing (`test-phylo-distributional.R`: `paths()` exposes the `x -> sigma(m)` edge and hides the phylo term; `indirect_effects()` returns a finite distribution-mediated effect), because the underlying machinery is generic rather than phylo-specific. **Does not cover:** the evolutionary parameter (lambda/alpha/kappa) is fixed by the caller on a grid, never jointly estimated with the SEM (explicitly out of scope — "file upstream" if needed); `drmTMB`'s own `phylo()` support is intercept-only for Gaussian/Poisson/NB2 (a `drmTMB` constraint, not a `drmSEM` one). |
| Ordinal (`cumulative_logit`) nodes | covered | `drm_node(family = drmTMB::cumulative_logit())` | `paths()` labels the edge `mu`/`logit` and never leaks the cutpoints; `dsep()` genuinely tests the claim (status `ok`) and Fisher's C is finite; effects decompose and close additively; latent-scale recovery **0.5009 / 0.9330** against true 0.500 / 0.900 at n = 800, seed 101 (`test-ordinal.R`, V-87..V-89b, 8 tests / 28 assertions). No family whitelist exists anywhere in `R/`, so this worked before it was tested — the gap was evidence, not capability. **Does not cover, and each is asserted as a limitation rather than left to be discovered:** `mu` is the **latent linear predictor, not `E[category]`** (measured range ≈(−3.2, 3.2) against categories 1–4), so `target = "mean"` reports a latent-scale effect **with no warning** (V-90); a non-mean target such as `target = "p_gt"` returns **exactly 0 for every quantity**, because `cumulative_logit` has no realized-value sampler and the mean fallback collapses both scenarios (V-91); and an ordinal node **cannot be distributional** — the family declares `dpars = "mu"` only, so no causal path can target its scale (V-92, drmTMB Issue 7). |
| Spatially-structured nodes | covered | `relmat(1 | site, K = <matrix>)` | A distance-kernel `relmat()` node forms a valid SEM with `site`/`K`/`relmat` stripped from `paths()`, and both `dsep()` (the augmented refit resolving `K` from the SEM's environment) and `indirect_effects()` run over it (`test-spatial.R`, V-93/V-94). `relmat(K = <any PSD matrix>)` is **strictly more flexible for spatial work than drmTMB's own `spatial()` marker**, whose only implemented kernel is a fixed exponential with a heuristic, non-estimated range and whose `mesh=` argument aborts as unimplemented — so `relmat()`, not `spatial()`, is the documented spatial route. **Does not cover:** the kernel is supplied by the caller, never estimated; there is no variogram or range-fitting helper. |
| Missing-data row alignment across nodes | covered | `drm_sem(na_action=)`, `attr(x, "alignment_issues")`, `check_sem()$nobs` | Until 2026-08-14 drmSEM had **no missing-data policy at all** — no `na.omit`, `complete.cases` or `na.action` anywhere in `R/` — so a piecewise SEM could be fitted on several different samples with nothing said, and `drm_fixed_design()` returned a design matrix shorter than `newdata` (because `model.matrix()` honours `na.action = na.omit`), which either raised `"number of items to replace is not a multiple of replacement length"` or, when the counts divided evenly, **recycled silently into a scrambled design matrix**. `na_action` now takes `"warn"` (default), `"common"` (one shared complete-case set, intersecting per-node row sets so unmodelled columns cost no rows) or `"fail"`; per-node counts surface in `print()`, `attr(x, "alignment_issues")` and `check_sem()`. A d-separation claim whose augmented refit changed the sample size reports `status = "n_mismatch"` and is excluded from Fisher's C. Evidence: `tests/testthat/test-missing-data.R`, 9 tests / 25 assertions, including an explicit assertion on the silent-recycling case (300 newdata rows against 150 complete) — a test asserting only "does not error" would have passed against that bug. **Does not cover:** incomplete **exogenous** predictors, which have no node model in the graph and remain governed by `na_action` rather than imputed. |
| Imputation models derived from the causal graph | partial | `drm_sem(impute = "auto")`, `imputation()`, `imputed()` | For each node with an incomplete **endogenous** parent, drmSEM derives that parent's imputation model from its own node formula and family and hands it to the engine as `mi()` + `impute_model()`; the user never writes an `impute_model()`. The conditioning set is therefore read off the DAG rather than chosen by folklore, and imputer and analysis model come from one graph. A node with **two incomplete Gaussian parents** emits two independent `mi()` terms (drmTMB 0.7.0 / #1086). A **Gamma** response with one Bernoulli parent emits (drmTMB #1088 `6e553879`, `mp-gamma-bernoulli`, drmSEM V-122). A **lognormal** response with one Bernoulli parent emits (drmTMB #1092 `7c104bbd5`, `mp-lognormal-bernoulli`, drmSEM V-123). A **beta_binomial** response with one Bernoulli parent emits (drmTMB #1094 `4c34c9bb`, `mp-beta-binomial-bernoulli`, drmSEM V-124). A **student** response with one Bernoulli parent emits (drmTMB #1096 `5fdf834c1`, `mp-student-bernoulli`, drmSEM V-125). An **nbinom2** response with one Gaussian parent emits (drmTMB #1095 `3c239a55e`, `mp-nbinom2-gaussian`, drmSEM V-126). A **zero-inflated Poisson** node (`family = poisson()` plus `zi ~ 1`) with one Bernoulli parent emits (drmTMB #1097 `9d4b63400`, `mp-zi-poisson-bernoulli`, drmSEM V-127; D-23: `mi()` in `mu` only; not a `zi_poisson` alias on V-80). Evidence: `tests/testthat/test-imputation.R` (V-77 identity; V-78 MAR bias reduction; V-79/79b/79c emit and fail-loud; V-80/80b/80d/80e/80f/80g/80h/80i family gate; V-82 two-parent identity; V-120 two-parent MAR recovery-to-truth; V-121 `uncertainty_status` tiers; V-122 Gamma × binary identity + MAR; V-123 lognormal × binary identity + MAR; V-124 beta_binomial × binary identity + MAR; V-125 student × binary identity + MAR; V-126 nbinom2 × Gaussian identity + MAR; V-127 ZIP × binary identity + MAR). **Does not cover, and these are the reasons it stays `partial`:** the SEM is piecewise, so the downstream node **re-estimates** the parent's model inside its own likelihood rather than sharing its estimates — imputation uncertainty is propagated *within* a node but **not across nodes**, and this must never be described as FIML across the SEM; `k > 2` and a non-Gaussian response with two incomplete parents still abort (remaining Issue 2); non-Gaussian responses other than nbinom2 still admit only a **binary** missing predictor, nbinom2 still refuses a non-Gaussian non-binary predictor, and responses outside gaussian/poisson/binomial/nbinom2/beta/gamma/lognormal/beta_binomial/student are refused. The measured one-parent claim is "recovers the intercept and reduces mediator-coefficient bias under outcome-dependent missingness", not "beats complete-case". See `docs/design/13-missing-data.md`. |
| Full-information Bayesian imputation, MCMC, priors | won't do | — | Out of scope by charter, not by omission. drmSEM never fits its own likelihood (`docs/design/00-charter.md`), and full-information imputation inside a joint model would require exactly that. Recorded here so the boundary is a stated design position rather than a silent absence. `because` (von Hardenberg et al. 2026) occupies that space; the two packages extend piecewise SEM along perpendicular axes. |
| Symbolic equation rendering via `symbolizer` (Suggests) | partial | `symbolize.drm_sem()`, `as_latex()`, `equations()`, `assumption_table()` | `symbolize.drm_sem()` walks a fitted SEM in topological order and dispatches `symbolizer::symbolize()` per node; the renderer methods collate per-node LaTeX/equations/assumptions with section headers. The integration is gated on `requireNamespace("symbolizer")` and `test-symbolize.R` correctly `skip_if_not_installed()`s around it. **`symbolizer` is not installed in this environment**, so those 2 tests were skipped in the fresh local run (2026-07-19) rather than confirmed passing; only the "errors when symbolizer is missing" branch was exercised here. Covers the model-specification layer only (per-node equations/families/links/assumptions); the SEM layer itself (DAG, d-separation, Fisher's C, the simulation-based effect calculus) has no closed-form symbolic representation by construction and is out of scope for this bridge. |
| DAG and effect plotting | covered | `plot.drm_sem()`, `plot.drm_effect()` | Directed-path, covariance-arc (double-headed residual / dashed higher-level), and composite-measurement-edge rendering, plus the legend-construction helper (`drm_path_legend()`, guaranteed not to drift from what is actually drawn), are tested via null-device renders in `test-plotting.R`; `plot.drm_effect()` (forest and stacked styles, via `ggplot2`, a Suggests dependency that is installed here) is tested for the multi-row, single-row, and no-decomposition cases. |

## Important limits

- `drmSEM` never fits its own likelihoods. Every quantity it reports — a path coefficient, a d-separation p-value, an effect, a correlation — traces back to one or more `drmTMB` fits; `drmSEM` supplies the graph, the decomposition, and the honesty checks around them.
- Effects are **conditional** (random effects at zero), not marginal/population-averaged; a causal path into a random-effect scale has no effect-decomposition entry today (OQ-9).
- An *unfitted* `drm_pair()` / `covary()` declaration still reports `estimate = NA`. After `drm_sem()` / `drm_psem()` attach a joint bivariate `drmTMB` fit, `rho12()` is a Wald table on the `tanh` link — still not a `y1 -> y2` path, and still not a global multivariate SEM.
- A feedback (cyclic) SEM gets an **equilibrium mean-only** `total_effects()`; it never gets an indirect/path decomposition, and node-wise fitting of a declared cycle is acknowledged as statistically inconsistent under simultaneity (a warning, not a fix).
- Calibration evidence (Type-I/power for d-sep, interval coverage for effects, model-selection recovery rate) is validated only for the specific DGP grids that were actually run (`docs/memory/OPEN_QUESTIONS.md` OQ-6, `docs/design/12-coverage-calibration.md` C-1/C-3); it does not generalize to arbitrary graphs, families, or sample sizes.
- `check_sem()` is covered by `test-diagnostics.R` (2026-08-15): row content against a live SEM, and every `print` warning branch against hand-built objects.
- The package is not CRAN-submittable while `drmTMB` and `symbolizer` remain GitHub `Remotes:` in `DESCRIPTION` (`NEWS.md`, "development version" section) — this is an explicit, acknowledged external release blocker, not an oversight.
- MAG m-separation (`latent =`) is a **graph projection** of *marginalised* confounding onto a piecewise observed SEM (`S = \emptyset`). It is not FIML, not a reflective measurement model, and does not support selection / conditioned latents. Residual compositionality is OQ-16. m-separation does not turn a `sigma` (or `zi`/`nu`) path into a mean effect.
- The R-only twin: there is no Julia twin or cross-language parity claim (unlike `hsquared`/`HSquared.jl`).

## Evidence anchors

- Interface and framing: `README.md` (engine/layer split, component-labelled paths, honest-effects framing, comparison to `lavaan`/`piecewiseSEM`/`glmmTMB`/`dsem`), `DESCRIPTION` (32 exports via `NAMESPACE`, `Suggests: drmTMB, ape, symbolizer, ggplot2`), `docs/design/00-charter.md` (scope, in/out-of-scope list), `docs/design/05-roadmap.md` (staged 0.1-0.5 plan, what shipped vs. what is engine-dependent and deferred).
- Fresh, first-party verification for this file (2026-08-28): a full local `devtools::test()` run with `drmTMB` **0.7.0** installed and `NOT_CRAN=true` — **1143 passing / 0 failing / 5 skips / 10 warnings** across **38** test files. Skips: unreachable `ape`-missing guard, 2 `symbolizer`-gated tests (`symbolizer` is not installed here), and V-127 / V-127b (this machine's installed `drmTMB` 0.7.0 predates engine #1097 ZIP `has_mi`; those passed on the #56 consumer-lift lane against that engine). Without `NOT_CRAN=true`, additional kernel tests are skipped as `skip_on_cran()` even though they need no engine.
- Core engine adapter and graph grammar: `R/extractors.R` (the sole file that touches a `drmTMB` object directly), `R/edges.R`, `R/drm_sem.R`, `R/drm_node.R`, `R/utils.R`.
- Effect engine: `R/effects.R`, `R/simulate_effects.R`, `R/effects_api.R`, `R/path_effects.R`.
- d-separation, MAG m-separation, diagnostics, standardization: `R/dsep.R`, `R/mag.R`, `R/latents.R`, `R/diagnostics.R`, `R/standardize.R`.
- Model comparison, composites, covariance edges, bivariate nodes, feedback, interop, phylogenetics, symbolizer bridge, plotting: `R/model_set.R`, `R/composite.R`, `R/covariances.R`, `R/pair.R`, `R/feedback.R`, `R/interop.R`, `R/phylo_cov.R`, `R/symbolize.R`, `R/plotting.R`.
- Regression coverage (38 files, 315 `test_that()` blocks, `tests/testthat/`): one or more `test-<area>.R` files per row above, including `test-mag.R` / `test-mag-basis.R` / `test-mag-dsep.R`; cross-cutting live-fit suites `test-integration.R`, `test-recovery.R`, `test-recovery-families.R`, `test-recovery-samplers.R`, `test-recovery-structural.R`, `test-calibration.R`.
- Stated caveats, open questions, and staged status: `NEWS.md` (per-release feature framing and honesty notes), `docs/memory/OPEN_QUESTIONS.md` (OQ-1 through OQ-17, the authoritative per-item open/resolved/partial ledger), `docs/design/11-validation-matrix.md` (the kernel vs. live-fit vs. calibration tier map with V-numbers), `docs/design/12-coverage-calibration.md` (the C-1..C-4 wave-2 calibration spec and current cache status), `docs/memory/CODEX_HANDOFF.md` (outstanding live-engine work items), `docs/memory/VALIDATION_LEDGER.md` (the dated per-V-number ledger).
