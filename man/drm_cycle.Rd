% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feedback.R
\name{drm_cycle}
\alias{drm_cycle}
\title{Declare a feedback (cyclic) motif}
\usage{
drm_cycle(...)
}
\arguments{
\item{...}{Two or more node names (strings) forming the feedback motif.}
}
\value{
A \code{drm_cycle} declaration object.
}
\description{
drmSEM rejects cycles by default. \code{drm_cycle()} explicitly names the nodes of
a feedback motif — the canonical case being a reciprocal pair \verb{y1 ⇄ y2} — so
that those nodes are \emph{allowed} to form a cycle while every undeclared cycle
stays a hard error. Pass declarations to \code{\link[=drm_sem]{drm_sem()}} / \code{\link[=drm_psem]{drm_psem()}} via their
\code{feedback} argument.
}
\details{
Declaring a motif relaxes the topological-order requirement (the motif is
treated as a single layer) and makes \code{\link[=basis_set]{basis_set()}} drop independence claims
among the motif's nodes. It does \strong{not} make node-wise fitting consistent:
under simultaneity, ordinary maximum likelihood per node is biased, so
\code{\link[=drm_sem]{drm_sem()}} warns when a declared cycle is fitted naively. Consistent
estimation (instrumental variables / a joint likelihood) is an engine
capability; equilibrium \strong{effects} from supplied coefficients are computed by
the fixed-point propagator. See \code{docs/design/10-cyclic-feedback.md}.
}
\examples{
drm_cycle("activity", "boldness")   # a reciprocal pair activity <-> boldness
}
\references{
\insertRef{Bollen1996}{drmSEM}

\insertRef{ForreMooij2017}{drmSEM}

\insertRef{Bollen1989}{drmSEM}
}
\seealso{
\code{\link[=cycles]{cycles()}}, \code{\link[=drm_sem]{drm_sem()}}.
}
