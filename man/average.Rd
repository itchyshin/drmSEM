% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_set.R
\name{average}
\alias{average}
\alias{average.drm_model_set}
\alias{average.drm_compare}
\title{CICc-weighted average of standardized path coefficients}
\usage{
average(object, ...)

\method{average}{drm_model_set}(object, ...)

\method{average}{drm_compare}(object, method = c("sd_x", "latent"), ...)
}
\arguments{
\item{object}{A \code{drm_compare} (from \code{\link[=compare]{compare()}}) or a \code{drm_model_set}.}

\item{method}{Standardization passed to \code{\link[=standardize]{standardize()}} (\code{"sd_x"} or
\code{"latent"}).}

\item{...}{Passed to \code{\link[=compare]{compare()}} when \code{object} is an unfitted model set.}
}
\value{
A data frame of averaged standardized paths with columns \code{from},
\code{to}, \code{component}, \code{std.estimate} (weighted mean) and \code{weight_sum} (total
weight of the models containing that path).
}
\description{
\code{average()} returns model-averaged standardized path coefficients across the
candidate set, weighting each model's \code{\link[=standardize]{standardize()}}d \code{\link[=paths]{paths()}} by its
Akaike weight. Coefficients are matched on \code{from}, \code{to} and \code{component}, so a
path present in only some candidates is averaged over the weight of the
models that contain it (conditional averaging). This mirrors phylopath's
\code{average()}.
}
\examples{
\dontrun{
average(cmp)
}
}
\seealso{
\code{\link[=compare]{compare()}}, \code{\link[=best]{best()}}, \code{\link[=standardize]{standardize()}}.
}
