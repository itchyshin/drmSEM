% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot.drm_effect}
\alias{plot.drm_effect}
\title{Plot an effect decomposition as a forest plot}
\usage{
\method{plot}{drm_effect}(x, style = c("forest", "stacked"), ...)
}
\arguments{
\item{x}{A \code{drm_effect} data frame from \code{\link[=indirect_effects]{indirect_effects()}},
\code{\link[=direct_effects]{direct_effects()}}, or \code{\link[=total_effects]{total_effects()}}.}

\item{style}{\code{"forest"} (default; one point-and-interval row per quantity) or
\code{"stacked"} (a single bar stacking \code{direct} + \code{mean_mediated} +
\code{distribution_mediated}, which sum to the total effect). \code{"stacked"} needs
the decomposition rows from \code{\link[=indirect_effects]{indirect_effects()}} and falls back to
\code{"forest"} if they are absent.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} object (invisibly printed by default).
}
\description{
Visualizes the output of \code{\link[=indirect_effects]{indirect_effects()}} (or \code{\link[=direct_effects]{direct_effects()}} /
\code{\link[=total_effects]{total_effects()}}) as a horizontal point-and-interval (forest) plot, with a
reference line at zero. This is the picture the rest of the SEM ecosystem does
not draw: \code{piecewiseSEM}, \code{dsem}, and \code{lavaan} plot the path diagram but leave
the direct / indirect / total \emph{decomposition} as a table. drmSEM separates the
\strong{distribution-mediated} contribution (the effect flowing through a mediator's
scale, zero-inflation, or shape) from the \strong{mean-mediated} part, so a path that
acts on dispersion rather than the mean is visible.
}
\details{
Requires \code{ggplot2} (Suggests); returns a \code{ggplot} object you can restyle.
}
\examples{
\dontrun{
sem <- drm_sem(
  size = drm_node(drmTMB::bf(size ~ temp + habitat, sigma ~ temp),
                  family = stats::gaussian()),
  abundance = drm_node(drmTMB::bf(abundance ~ size + temp, zi ~ habitat),
                       family = drmTMB::nbinom2()),
  data = dat)
eff <- indirect_effects(sem, from = "temp", to = "abundance", through = "size")
plot(eff, style = "forest")
}
}
\references{
\insertRef{Pearl2001}{drmSEM}

\insertRef{Imai2010}{drmSEM}

\insertRef{VanderWeele2015}{drmSEM}
}
