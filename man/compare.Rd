% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_set.R
\name{compare}
\alias{compare}
\alias{compare.drm_model_set}
\title{Fit and rank a set of candidate causal models}
\usage{
compare(object, data, family = NULL, criterion = c("CBIC", "CICc"), ...)

\method{compare}{drm_model_set}(object, data, family = NULL, criterion = c("CBIC", "CICc"), ...)
}
\arguments{
\item{object}{A \code{drm_model_set}.}

\item{data}{A data frame supplied to every node fit.}

\item{family}{Optional. A single \code{drmTMB} family applied to every node, or a
named list of families keyed by node (response) name. \code{NULL} uses the
\code{\link[=drm_node]{drm_node()}} default (Gaussian) for every node.}

\item{criterion}{Ranking criterion. \code{"CBIC"} is the default BIC-style
parsimony criterion; \code{"CICc"} gives the phylopath-style support ranking.}

\item{...}{Further arguments passed on to each \code{\link[=drm_sem]{drm_sem()}} fit.}
}
\value{
A \code{drm_compare} object: a data frame with one row per candidate and
columns \code{model}, \code{fisher_c}, \code{df}, \code{p.value}, \code{k}, \code{n}, \code{CICc}, \code{dCICc},
\code{wCICc}, \code{CBIC}, \code{dCBIC}, \code{wCBIC}, and \code{weight}, sorted by the selected
\code{criterion}. \code{weight} is the weight for the selected criterion, so
\code{\link[=average]{average()}} follows the same ranking rule. The fitted \code{drm_sem} objects and
per-model d-separation tables are carried in attributes for
\code{\link[=best]{best()}}/\code{\link[=average]{average()}}.
}
\description{
\code{compare()} fits every candidate \code{\link[=drm_dag]{drm_dag()}} in a \code{\link[=drm_model_set]{drm_model_set()}} with
\code{\link[=drm_sem]{drm_sem()}} (one node per response, using a per-node or shared \code{family}),
runs \code{\link[=dsep]{dsep()}}/\code{\link[=fisher_c]{fisher_c()}} on each, and ranks the candidates by an
information criterion built on Fisher's C. This is \code{drmSEM}'s analogue of
phylopath's \code{phylo_path()} (van der Bijl 2018).
}
\details{
The default ranking statistic is
\deqn{\mathrm{CBIC} = C + k\log(n),}
where \eqn{C} is Fisher's C statistic from the model's d-separation test,
\eqn{k} is the number of estimated fixed-effect coefficients across all
nodes (counted via \code{\link[=paths]{paths()}}), and \eqn{n} is the number of observations.
CBIC uses a BIC-style penalty and is the default because it is more
conservative about extra paths. \code{criterion = "CICc"} instead ranks by
\deqn{\mathrm{CICc} = C + 2k\,\frac{n}{n - k - 1},}
\eqn{\Delta}CICc is the difference from the best (lowest-CICc) model and the
CICc weight is \eqn{\exp(-\tfrac12\,\Delta\mathrm{CICc})}, normalised to sum
to one. As \eqn{n \to \infty} the correction term tends to \eqn{2k}, so CICc
reduces to the usual \eqn{C + 2k}.

\strong{Citation and novelty.} \code{CICc} is the C-statistic information criterion of
Shipley (2013), used as the default in \code{phylopath} (van der Bijl 2018).
\code{CBIC} is \emph{introduced here} as a BIC-style analogue: it combines Shipley's C
statistic with the BIC penalty \eqn{k\log(n)} (Schwarz 1978). As far as we
can find, no published criterion of this exact form has been named or
defaulted to in the piecewise-SEM / d-separation literature; users who want
to stay on the established AIC-style ranking should set
\code{criterion = "CICc"}. Information-criterion weights for multimodel inference
follow Burnham & Anderson (2002).
}
\examples{
\dontrun{
models <- drm_model_set(
  direct   = drm_dag(fitness ~ temp + size),
  mediated = drm_dag(size ~ temp, fitness ~ size + temp)
)
cmp <- compare(models, data = dat,
               family = list(size = stats::gaussian(),
                             fitness = stats::gaussian()))
cmp
best(cmp)
average(cmp)
}
}
\references{
\insertRef{Shipley2013}{drmSEM}

\insertRef{Schwarz1978}{drmSEM}

\insertRef{Burnham2002}{drmSEM}

\insertRef{vanderBijl2018}{drmSEM}

\insertRef{Lefcheck2016}{drmSEM}
}
\seealso{
\code{\link[=best]{best()}}, \code{\link[=average]{average()}}, \code{\link[=drm_model_set]{drm_model_set()}}.
}
