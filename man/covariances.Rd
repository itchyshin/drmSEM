% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariances.R
\name{covariances}
\alias{covariances}
\alias{covariances.drm_sem}
\title{Covariance edges of a distributional SEM (residual rho12 and higher-level corpair)}
\usage{
covariances(object, ...)

\method{covariances}{drm_sem}(object, ...)
}
\arguments{
\item{object}{A \code{drm_sem} object.}

\item{...}{Unused.}
}
\value{
A \code{drm_covariances} data frame with columns \code{y1}, \code{y2}, \code{class}
(\code{"residual"} / \code{"higher_level"}), \code{level}, \code{structure}, \code{label}.
}
\description{
Returns the \strong{covariance allowances} declared via \code{\link[=covary]{covary()}} — double-headed
arcs that are deliberately kept \emph{separate} from \code{\link[=paths]{paths()}} (which stays
directed-only, including any \code{x -> rho12} directed path into the correlation
component). Residual (\code{rho12}, within-observation) and higher-level (\code{corpair},
between-unit random-effect) edges are reported together with a \code{class} column
that distinguishes them; they answer different biological questions and are
never collapsed. A declared edge also makes \code{\link[=basis_set]{basis_set()}} / \code{\link[=dsep]{dsep()}} drop the
\code{y1 _||_ y2} independence claim.
}
\examples{
\dontrun{
sem <- drm_sem(
  activity = drm_node(drmTMB::bf(activity ~ x), family = stats::gaussian()),
  boldness = drm_node(drmTMB::bf(boldness ~ x), family = stats::gaussian()),
  data = dat,
  covariances = covary("activity", "boldness"))
covariances(sem)   # the residual rho12 edge, reported separately from paths()
}
}
\seealso{
\code{\link[=covary]{covary()}}, \code{\link[=paths]{paths()}}, \code{\link[=basis_set]{basis_set()}}.
}
