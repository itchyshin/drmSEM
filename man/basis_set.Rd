% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsep.R
\name{basis_set}
\alias{basis_set}
\alias{basis_set.drm_sem}
\title{Basis set of independence claims for a distributional SEM}
\usage{
basis_set(object, ...)

\method{basis_set}{drm_sem}(object, ...)
}
\arguments{
\item{object}{A \code{drm_sem} object.}

\item{...}{Unused.}
}
\value{
A data frame with columns \code{claim}, \code{x}, \code{y}, \code{given} (comma-separated
conditioning set = Y's parents).
}
\description{
The basis set is the collection of non-adjacent variable pairs (X, Y) where Y
is endogenous and X is causally no later than Y. Each claim asserts that X has
\strong{no effect on any modelled distributional component of Y}, conditional on
Y's existing parents. This any-component reading is drmSEM's definition of a
missing arrow (see \code{docs/design/03-dsep.md}).
}
\details{
A covariance edge declared with \code{\link[=covary]{covary()}} (a residual \code{rho12} or higher-level
\code{corpair} arc) is an allowance that the two responses stay associated, so the
\verb{y1 _||_ y2} claim is dropped from the basis set (Shipley's bidirected-edge
rule; OQ-14). A declared feedback motif (\code{\link[=drm_cycle]{drm_cycle()}}, 0.5) likewise drops
independence claims among its nodes — DAG d-separation does not hold across a
cycle, and the goodness-of-fit test is scoped to the acyclic part until
sigma-separation lands.

The any-component reading of an independence claim is a \code{drmSEM} choice on top
of the local-likelihood d-separation framework of Shipley; the d-separation
graphical criterion itself is due to Pearl, and the bidirected-edge handling
of declared residual correlations follows Shipley's path-analysis treatment.
}
\examples{
\dontrun{
sem <- drm_sem(
  size = drm_node(drmTMB::bf(size ~ temp + habitat, sigma ~ temp),
                  family = stats::gaussian()),
  abundance = drm_node(drmTMB::bf(abundance ~ size + temp, zi ~ habitat),
                       family = drmTMB::nbinom2()),
  data = dat)
basis_set(sem)
}
}
\references{
\insertRef{Shipley2000}{drmSEM}

\insertRef{Shipley2009}{drmSEM}

\insertRef{Shipley2016}{drmSEM}

\insertRef{Pearl2009}{drmSEM}

\insertRef{Lefcheck2016}{drmSEM}
}
