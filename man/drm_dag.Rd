% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_set.R
\name{drm_dag}
\alias{drm_dag}
\title{Specify a candidate causal model (an unfitted DAG)}
\usage{
drm_dag(...)
}
\arguments{
\item{...}{One formula per endogenous node. A bare \code{response ~ predictors}
formula, or a \code{\link[drmTMB:bf]{drmTMB::bf()}} object for distributional parts. The node name
is taken from the response.}
}
\value{
A \code{drm_dag} object: a list of captured node formulas.
}
\description{
\code{drm_dag()} captures a set of node formulas as one candidate causal model in
a confirmatory model set, without fitting anything. Each argument is a node:
a plain \code{response ~ predictors} formula, or a \code{\link[drmTMB:bf]{drmTMB::bf()}} /
\code{\link[drmTMB:drm_formula]{drmTMB::drm_formula()}} for a distributional node (so a candidate may posit
arrows into \code{sigma}, \code{zi}, \code{hu}, ... as well as the mean). The response of
each formula names the node. Models are fitted later by \code{\link[=compare]{compare()}}.
}
\examples{
\dontrun{
# mean-only candidate
drm_dag(size ~ temp, fitness ~ size + temp)
# distributional candidate: temp also drives residual scale of size
drm_dag(drmTMB::bf(size ~ temp, sigma ~ temp), fitness ~ size)
}
}
\seealso{
\code{\link[=drm_model_set]{drm_model_set()}}, \code{\link[=compare]{compare()}}.
}
