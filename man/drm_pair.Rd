% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pair.R
\name{drm_pair}
\alias{drm_pair}
\title{Declare a bivariate (joint two-response) node}
\usage{
drm_pair(
  formula1,
  formula2,
  rho12 = NULL,
  family = stats::gaussian(),
  family2 = family,
  level = NULL,
  names = NULL
)
}
\arguments{
\item{formula1, formula2}{Two two-sided response formulas (e.g.
\code{activity ~ x + (1 | id)} and \code{boldness ~ x + (1 | id)}). Random-effect bar
groups shared between the two declare the higher-level \code{corpair} edge.}

\item{rho12}{Optional one-sided formula (e.g. \code{~ x}) giving predictors of the
residual correlation on its link (\code{tanh}) scale — a directed path into the
\code{rho12} component. \code{NULL} (default) declares a constant residual correlation.}

\item{family, family2}{\code{drmTMB}/\code{stats} families for the two responses.
\code{family2} defaults to \code{family} (a homogeneous bivariate node).}

\item{level}{Higher-level (\code{corpair}) grouping. \code{NULL} (default) auto-detects
grouping factors common to both formulas; a string forces a specific level;
\code{NA} suppresses the \code{corpair} edge (residual \code{rho12} only).}

\item{names}{Optional length-2 character vector of node names, defaulting to
the two response labels.}
}
\value{
A \code{drm_pair} declaration object.
}
\description{
\code{drm_pair()} records a \strong{bivariate node}: two responses to be fitted jointly
with a residual correlation \code{rho12} (the within-observation coupling that
remains after each response's mean and scale), optionally modelled as a
function of predictors (\code{rho12 ~ x}, a directed path \emph{into} the correlation
component), and — when the two response formulas share a grouping level — a
higher-level random-effect correlation (\code{corpair}). It is the bivariate
counterpart of \code{\link[=drm_node]{drm_node()}}.
}
\details{
\strong{This is the declaration grammar, not the fit.} drmSEM is piecewise and the
dev lane has no engine, so \code{drm_pair()} does not estimate \code{rho12}: it validates
the declaration and bridges it onto the shipped covariance-edge grammar
(\code{\link[=covary]{covary()}} / \code{\link[=covariances]{covariances()}}), so the residual (\code{rho12}) and higher-level
(\code{corpair}) arcs are reported separately from \code{\link[=paths]{paths()}} and respected by
\code{\link[=basis_set]{basis_set()}} / \code{\link[=dsep]{dsep()}}. Estimating \code{rho12} inside one joint \code{drmTMB} model —
and reading it back through \code{\link[=rho12]{rho12()}} / \code{\link[=corpairs]{corpairs()}} — is the 0.4 engine
deliverable (see \code{docs/design/07-bivariate-covariance-edges.md}). The declared
estimates are \code{NA} until a live bivariate fit is supplied; \code{drm_pair()} never
fabricates a correlation.
}
\examples{
# A bivariate node: two responses sharing an `id` grouping, with the residual
# correlation itself modelled as a function of `x`.
pair <- drm_pair(
  activity ~ x + (1 | id),
  boldness ~ x + (1 | id),
  rho12 = ~ x
)
pair
rho12(pair)      # declared residual edge (estimate NA -> needs a joint fit)
corpairs(pair)   # declared higher-level edge at the shared `id` level
}
\seealso{
\code{\link[=covary]{covary()}}, \code{\link[=rho12]{rho12()}}, \code{\link[=corpairs]{corpairs()}}, \code{\link[=drm_node]{drm_node()}},
\code{\link[=drm_expand_pair]{drm_expand_pair()}}.
}
