% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/symbolize.R
\name{symbolize.drm_sem}
\alias{symbolize.drm_sem}
\title{Symbolize a distributional piecewise SEM}
\usage{
\method{symbolize}{drm_sem}(fit, symbols = NULL, units = NULL, context = NULL, ...)
}
\arguments{
\item{fit}{A \code{drm_sem} object.}

\item{symbols, units, context}{Passed through to each per-node
\code{\link[symbolizer:symbolize]{symbolizer::symbolize()}} call. See \code{?symbolizer::symbolize} for the
shape (named character vectors keyed by variable name).}

\item{...}{Passed through to each per-node \code{\link[symbolizer:symbolize]{symbolizer::symbolize()}} call
(for example, \code{ci_method = "profile"} for \code{drmTMB} nodes).}
}
\value{
A \code{symbolized_drm_sem} object (a list with elements \code{parts},
\code{edges}, \code{order}, \code{dpars}, \code{metadata}).
}
\description{
Builds a \code{symbolized_drm_sem} from a fitted \code{\link[=drm_sem]{drm_sem()}} / \code{\link[=drm_psem]{drm_psem()}} object
by walking the topological-order node list and calling \code{\link[symbolizer:symbolize]{symbolizer::symbolize()}}
on each node's underlying \code{drmTMB} fit. The result is a list of per-node
\code{symbolized_model}s plus the SEM's graph metadata (edges, topological order,
modelled distributional components per node), so the rendering generics
(\code{\link[symbolizer:as_latex]{symbolizer::as_latex()}}, \code{\link[symbolizer:equations]{symbolizer::equations()}},
\code{\link[symbolizer:assumption_table]{symbolizer::assumption_table()}}) can collate the SEM's equations as one
document.
}
\details{
This is the \strong{model-specification} layer: per-node distributional equations
(location, scale, shape, zero-inflation, hurdle, residual correlation),
families, links, and assumptions. The \strong{SEM layer} — DAG plotting,
d-separation, Fisher's C, the simulation-based effect calculus — stays
with the existing \code{drmSEM} machinery; symbolizer does not attempt to render
the (simulation-based) distribution-mediated effect.

Requires the \href{https://itchyshin.github.io/symbolizer/}{\code{symbolizer}} package
(a \code{Suggests}).
}
\examples{
\dontrun{
sem <- drm_sem(
  size      = drm_node(drmTMB::bf(size ~ temp + habitat, sigma ~ temp),
                       family = stats::gaussian()),
  abundance = drm_node(drmTMB::bf(abundance ~ size + temp, zi ~ habitat),
                       family = drmTMB::nbinom2()),
  data = dat
)
sym <- symbolize(sem)
as_latex(sym)            # publication-ready equations, node-by-node
equations(sym)           # raw equation list
assumption_table(sym)    # per-node families, links, deferred components
}
}
\references{
\insertRef{Shipley2009}{drmSEM}

\insertRef{Lefcheck2016}{drmSEM}

\insertRef{Brooks2017}{drmSEM}

\insertRef{Rigby2005}{drmSEM}
}
\seealso{
\code{\link[symbolizer:symbolize]{symbolizer::symbolize()}}, \code{\link[symbolizer:as_latex]{symbolizer::as_latex()}},
\code{\link[symbolizer:equations]{symbolizer::equations()}}, \code{\link[symbolizer:assumption_table]{symbolizer::assumption_table()}}.
}
