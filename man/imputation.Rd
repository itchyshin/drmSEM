% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputation.R
\name{imputation}
\alias{imputation}
\alias{imputation.drm_sem}
\title{Imputation models drmSEM derived from the graph}
\usage{
imputation(object, ...)

\method{imputation}{drm_sem}(object, ...)
}
\arguments{
\item{object}{A \code{drm_sem} object.}

\item{...}{Unused.}
}
\value{
A data frame with columns \code{node}, \code{variable}, \code{model} and \code{family},
with zero rows when no imputation was derived.
}
\description{
Reports the per-node imputation models \code{\link[=drm_sem]{drm_sem()}} built from the DAG when
called with \code{impute = "auto"}. Each row is one node whose incomplete parent
was imputed from that parent's own node model.
}
\examples{
\dontrun{
sem <- drm_sem(
  m = drm_node(drmTMB::bf(m ~ x)),
  y = drm_node(drmTMB::bf(y ~ m + x)),
  data = dat, impute = "auto"
)
imputation(sem)
}
}
\seealso{
\code{\link[=drm_sem]{drm_sem()}}, \code{\link[=check_sem]{check_sem()}}.
}
