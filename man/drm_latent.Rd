% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latents.R
\name{drm_latent}
\alias{drm_latent}
\title{Declare a latent construct (MIMIC, reflective, or formative)}
\usage{
drm_latent(
  name,
  indicators,
  causes = NULL,
  type = c("mimic", "reflective", "formative"),
  identification = c("marker", "unit_variance"),
  marker = NULL,
  method = c("pca", "fa", "fixed"),
  weights = NULL,
  data = NULL,
  standardize = FALSE
)
}
\arguments{
\item{name}{Name of the latent construct column to create.}

\item{indicators}{Character vector of indicator column names (length >= 2), or a
list of \code{\link[=drm_indicator]{drm_indicator()}} declarations.}

\item{causes}{Optional character vector of structural cause variables for
MIMIC constructs.}

\item{type}{Type of construct: \code{"mimic"} (default; causes -> latent -> indicators),
\code{"reflective"} (latent -> indicators), or \code{"formative"} (indicators -> construct).}

\item{identification}{Identification constraint: \code{"marker"} (unit loading on
marker indicator, \eqn{\lambda_{\text{marker}} = 1}) or \code{"unit_variance"}
(\eqn{\text{Var}(\eta) = 1}). Defaults to \code{"marker"}.}

\item{marker}{Optional name of the marker indicator for \code{"marker"} identification
(defaults to the indicator with \code{marker = TRUE} or the first indicator).}

\item{method}{Measurement estimation method: \code{"pca"} (first principal component /
factor score), \code{"fa"} (1-factor analysis), or \code{"fixed"} (user weights).}

\item{weights}{Optional numeric weights for \code{method = "fixed"}.}

\item{data}{Optional data frame containing indicator and cause columns. If provided,
measurement structure and loadings are estimated immediately.}

\item{standardize}{Logical. If \code{TRUE}, materialized latent score is standardized
(mean 0, sd 1). Default \code{FALSE}.}
}
\value{
A \code{drm_latent} declaration object (inheriting from \code{drm_composite}).
}
\description{
Declares a latent construct within the piecewise SEM framework.
Supports MIMIC (Multiple Indicators, Multiple Causes), reflective (latent factor
with multiple indicators), and formative (composite) constructs.
}
\details{
In piecewise SEM, measurement equations and structural causes are estimated
piece-by-piece with sign-alignment and marker/unit-variance identification.
Latent scores are materialized into \code{data} before fitting, allowing structural
nodes to use the latent construct as an ordinary predictor or response in any
\code{\link[=drm_node]{drm_node()}} formula. Indicator loadings are reported by \code{\link[=loadings]{loadings()}} and kept
strictly separate from structural \code{\link[=paths]{paths()}}.
}
\examples{
dat <- data.frame(
  x1 = rnorm(50), x2 = rnorm(50),
  y1 = rnorm(50), y2 = rnorm(50), y3 = rnorm(50)
)
# MIMIC construct with marker identification:
lat <- drm_latent("size", indicators = c("y1", "y2", "y3"), causes = c("x1", "x2"), data = dat)
print(lat)
}
\references{
\insertRef{JoreskogGoldberger1975}{drmSEM}

\insertRef{Bollen1989}{drmSEM}

\insertRef{Raykov1997}{drmSEM}

\insertRef{Grace2008}{drmSEM}
}
\seealso{
\code{\link[=drm_indicator]{drm_indicator()}}, \code{\link[=drm_composite]{drm_composite()}}, \code{\link[=loadings]{loadings()}}, \code{\link[=drm_sem]{drm_sem()}}.
}
