% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop.R
\name{as_lavaan}
\alias{as_lavaan}
\alias{as_lavaan.drm_sem}
\alias{as_lavaan.drm_dag}
\title{Export a distributional SEM as lavaan model syntax}
\usage{
as_lavaan(object, ...)

\method{as_lavaan}{drm_sem}(object, ...)

\method{as_lavaan}{drm_dag}(object, ...)
}
\arguments{
\item{object}{A \code{drm_sem} (from \code{\link[=drm_sem]{drm_sem()}} / \code{\link[=drm_psem]{drm_psem()}}) or a \code{drm_dag}
(from \code{\link[=drm_dag]{drm_dag()}}).}

\item{...}{Unused.}
}
\value{
A length-1 character string of class \code{drm_lavaan} (its print method
\code{cat()}s the syntax). The \code{dropped} attribute lists any non-\code{mu} component
paths that lavaan syntax could not represent.
}
\description{
\code{as_lavaan()} renders the \strong{mean structure} of a drmSEM graph as a
lavaan-style model-syntax string: one \code{y ~ x1 + x2} regression line per
endogenous node (from the variable-level directed edges) and one \code{y1 ~~ y2}
line per declared covariance edge (\code{\link[=covariances]{covariances()}}).
}
\details{
This is \emph{graph interchange}, not a fitting bridge — drmSEM never fits its own
likelihoods and does not call lavaan. \strong{Honesty:} lavaan model syntax cannot
express a distributional-component path (an arrow into \code{sigma}, \code{zi}, \code{nu},
\code{hu}, \code{sd(group)}, or \code{rho12}). Such paths are therefore \strong{collapsed away},
never silently emitted as a mean (\code{~}) regression. Every dropped non-\code{mu} path
is attached as a \code{dropped} attribute and reported once via a \code{cli} message, so
the loss is explicit.
}
\examples{
# From an unfitted candidate DAG (no engine needed):
dag <- drm_dag(size ~ temp, abundance ~ size + temp)
as_lavaan(dag)
}
\references{
\insertRef{Rosseel2012}{drmSEM}
}
\seealso{
\code{\link[=from_lavaan]{from_lavaan()}}, \code{\link[=as_dot]{as_dot()}}, \code{\link[=covariances]{covariances()}}.
}
