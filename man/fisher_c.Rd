% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsep.R
\name{fisher_c}
\alias{fisher_c}
\alias{fisher_c.drm_sem}
\alias{fisher_c.drm_dsep}
\title{Fisher's C statistic for a fitted distributional SEM}
\usage{
fisher_c(object, ...)

\method{fisher_c}{drm_sem}(object, ...)

\method{fisher_c}{drm_dsep}(object, ...)
}
\arguments{
\item{object}{A \code{drm_sem} object or the result of \code{\link[=dsep]{dsep()}}.}

\item{...}{Unused.}
}
\value{
A one-row data frame with \code{fisher_c}, \code{df}, \code{n_claims}, \code{p.value}.
}
\description{
Combines the independence-claim p-values from \code{\link[=dsep]{dsep()}} into Fisher's C,
\code{C = -2 * sum(log(p))}, which is chi-squared with \verb{2k} degrees of freedom
under the hypothesis that all missing arrows are absent. A small p-value
indicates the DAG omits a needed path.
}
\details{
This is the Fisher-combined goodness-of-fit test introduced by Shipley (2000)
for path models on directed acyclic graphs and extended to generalized
multilevel models by Shipley (2009); \code{piecewiseSEM} (Lefcheck 2016) is the
established R implementation. The construction itself is unchanged here; what
differs is that the p-values being combined are the any-component
likelihood-ratio claims of \code{\link[=dsep]{dsep()}} rather than mean-only claims.
}
\examples{
\dontrun{
sem <- drm_sem(
  size = drm_node(drmTMB::bf(size ~ temp + habitat, sigma ~ temp),
                  family = stats::gaussian()),
  abundance = drm_node(drmTMB::bf(abundance ~ size + temp, zi ~ habitat),
                       family = drmTMB::nbinom2()),
  data = dat)
fisher_c(sem)
}
}
\references{
\insertRef{Shipley2000}{drmSEM}

\insertRef{Shipley2009}{drmSEM}

\insertRef{Lefcheck2016}{drmSEM}
}
