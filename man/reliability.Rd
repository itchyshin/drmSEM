% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composite.R
\name{reliability}
\alias{reliability}
\alias{reliability.drm_sem}
\title{Construct reliability summary for composite and latent constructs}
\usage{
reliability(object, ...)

\method{reliability}{drm_sem}(object, ...)
}
\arguments{
\item{object}{A \code{drm_sem} object.}

\item{...}{Unused.}
}
\value{
A \code{drm_reliability} data frame: \code{composite}, \code{type}, \code{method},
\code{n_indicators}, \code{alpha}, \code{rho}, \code{ave}, \code{prop_var} (one row per construct;
empty when there are no constructs).
}
\description{
Per-construct reliability diagnostics for \code{\link[=drm_composite]{drm_composite()}} and \code{\link[=drm_latent]{drm_latent()}}
constructs in a SEM: \code{alpha} (Cronbach's alpha), \code{rho} (Raykov's rho /
composite reliability), \code{ave} (average variance extracted -- the mean squared
standardized loading), and \code{prop_var} (proportion of variance explained).
}
\examples{
\dontrun{
sem <- drm_sem(
  fitness = drm_node(drmTMB::bf(fitness ~ body_size), family = stats::gaussian()),
  data = dat,
  composites = drm_composite("body_size", c("len", "mass"), data = dat))
reliability(sem)
}
}
\seealso{
\code{\link[=loadings]{loadings()}}, \code{\link[=drm_composite]{drm_composite()}}, \code{\link[=drm_latent]{drm_latent()}}.
}
