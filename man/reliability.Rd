% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composite.R
\name{reliability}
\alias{reliability}
\alias{reliability.drm_sem}
\title{Construct reliability summary for composite constructs}
\usage{
reliability(object, ...)

\method{reliability}{drm_sem}(object, ...)
}
\arguments{
\item{object}{A \code{drm_sem} object.}

\item{...}{Unused.}
}
\value{
A \code{drm_reliability} data frame: \code{composite}, \code{method},
\code{n_indicators}, \code{alpha}, \code{ave}, \code{prop_var} (one row per construct; empty when
there are no composites).
}
\description{
Per-construct reliability diagnostics for the \code{\link[=drm_composite]{drm_composite()}} constructs in a
SEM: \code{alpha} (Cronbach's alpha, internal consistency), \code{ave} (average variance
extracted -- the mean squared standardized loading), and \code{prop_var} (the
first principal-component proportion of variance, PCA composites only).
}
\details{
These are \strong{reflective-measurement} diagnostics: they assess whether the
indicator set hangs together as a reflective scale. A formative composite is
defined by its weights regardless of these values, so treat them as a
convenience check on the indicator set, not a requirement the construct must
meet. \code{ave}/\code{alpha} can be low (or \code{alpha} negative) when indicators are not
positively correlated, which is reported honestly rather than clamped.
}
\examples{
\dontrun{
sem <- drm_sem(
  fitness = drm_node(drmTMB::bf(fitness ~ body_size), family = stats::gaussian()),
  data = dat,
  composites = drm_composite("body_size", c("len", "mass"), data = dat))
reliability(sem)
}
}
\seealso{
\code{\link[=loadings]{loadings()}}, \code{\link[=drm_composite]{drm_composite()}}.
}
