% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composite.R
\name{drm_composite}
\alias{drm_composite}
\title{Declare a composite construct from observed indicators}
\usage{
drm_composite(name, indicators, weights = NULL, method = c("fixed", "pca"), data)
}
\arguments{
\item{name}{Name of the construct column to create. Must not collide with an
existing data column or node name.}

\item{indicators}{Character vector (length >= 2) of numeric indicator columns
present in \code{data}.}

\item{weights}{Optional numeric weights for \code{method = "fixed"} (defaults to
equal weights \code{1/k}). Ignored for \code{method = "pca"}.}

\item{method}{\code{"fixed"} (weighted sum of raw indicators) or \code{"pca"} (first
principal-component score of the scaled indicators).}

\item{data}{A data frame containing the \code{indicators} (used to validate them
and, for \code{"pca"}, to derive the loadings).}
}
\value{
A \code{drm_composite} declaration object.
}
\description{
A composite construct is a deterministic index built from two or more observed
indicator columns and materialized as a single column \emph{before} fitting, so it
can be used as an ordinary predictor or response in a node formula. With
\code{method = "fixed"} the construct is a weighted sum of the (raw) indicators;
with \code{method = "pca"} it is the first principal-component score of the scaled
indicators. The indicator loadings are recorded and reported by \code{\link[=loadings]{loadings()}}.
}
\details{
This is the \emph{formative / composite} construct (indicators define the
construct), which stays fully within drmSEM's piecewise, likelihood-based core.
\emph{Reflective} latent variables (a latent common cause estimated through a
measurement model) require a joint likelihood drmTMB does not fit piecewise and
are out of scope here; see \code{docs/design/09-latent-variables.md}.
}
\seealso{
\code{\link[=loadings]{loadings()}}, \code{\link[=drm_sem]{drm_sem()}}.
}
