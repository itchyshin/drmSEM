% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drm_sem.R
\name{drm_psem}
\alias{drm_psem}
\alias{summary.drm_sem}
\title{Assemble a distributional piecewise SEM from fitted drmTMB models}
\usage{
drm_psem(..., data = NULL, covariances = NULL, composites = NULL)

\method{summary}{drm_sem}(object, ...)
}
\arguments{
\item{...}{Named fitted \code{drmTMB} objects, one per endogenous node. The name
is the node identifier used in path queries; predictors are matched to a
node by its name or its response variable.}

\item{data}{The data frame all nodes were fitted to. Defaults to the data of
the first node.}

\item{covariances}{Optional \code{\link[=covary]{covary()}} declaration(s) (one object or a list)
recording residual (\code{rho12}) or higher-level (\code{corpair}) covariance edges
between responses. These are reported by \code{\link[=covariances]{covariances()}} and respected by
\code{\link[=basis_set]{basis_set()}} / \code{\link[=dsep]{dsep()}}, but never enter \code{\link[=paths]{paths()}} or the effect
decomposition.}

\item{composites}{Optional \code{\link[=drm_composite]{drm_composite()}} declaration(s). For \code{drm_psem()}
the construct column(s) must already be present in the data the nodes were
fitted on; the declarations are recorded so \code{\link[=loadings]{loadings()}} can report them.}

\item{object}{A \code{drm_sem} object.}
}
\value{
A \code{drm_sem} object.
}
\description{
\code{drm_psem()} is the piecewiseSEM-style core: you fit each endogenous node
yourself with \code{\link[drmTMB:drmTMB]{drmTMB::drmTMB()}} and pass the fitted models. drmSEM extracts
the component-labelled graph, validates it as a DAG, and provides path
tables, d-separation tests, and simulation-based effects on top.
}
\details{
Fit nodes with \code{control = drmTMB::drm_control(se = TRUE)} so that \code{\link[=vcov]{vcov()}},
Wald intervals, and d-separation refits are available.
}
\examples{
\dontrun{
ctrl <- drmTMB::drm_control(se = TRUE)
size_fit <- drmTMB::drmTMB(
  drmTMB::bf(size ~ temp + habitat, sigma ~ temp),
  family = stats::gaussian(), data = dat, control = ctrl)
abundance_fit <- drmTMB::drmTMB(
  drmTMB::bf(abundance ~ size + temp, zi ~ habitat),
  family = drmTMB::nbinom2(), data = dat, control = ctrl)
sem <- drm_psem(size = size_fit, abundance = abundance_fit, data = dat)
paths(sem)
}
}
\seealso{
\code{\link[=drm_sem]{drm_sem()}} for the declarative interface that fits nodes for you.
}
