% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feedback.R
\name{cycles}
\alias{cycles}
\alias{cycles.drm_sem}
\title{Declared feedback motifs of a distributional SEM}
\usage{
cycles(object, ...)

\method{cycles}{drm_sem}(object, ...)
}
\arguments{
\item{object}{A \code{drm_sem} object.}

\item{...}{Unused.}
}
\value{
A \code{drm_cycles} data frame with columns \code{motif} and \code{node}.
}
\description{
Returns the feedback (cyclic) motifs declared via \code{\link[=drm_cycle]{drm_cycle()}} and passed to
\code{\link[=drm_sem]{drm_sem()}} / \code{\link[=drm_psem]{drm_psem()}} through \verb{feedback =}. Each motif is a set of nodes
explicitly allowed to form a cycle; every undeclared cycle remains an error.
}
\examples{
\dontrun{
sem <- drm_psem(activity = a_fit, boldness = b_fit, data = dat,
                feedback = drm_cycle("activity", "boldness"))
cycles(sem)
}
}
\seealso{
\code{\link[=drm_cycle]{drm_cycle()}}.
}
