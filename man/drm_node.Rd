% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drm_node.R
\name{drm_node}
\alias{drm_node}
\title{Specify one endogenous node of a distributional SEM}
\usage{
drm_node(formula, family = stats::gaussian(), ...)
}
\arguments{
\item{formula}{A \code{\link[drmTMB:bf]{drmTMB::bf()}} / \code{\link[drmTMB:drm_formula]{drmTMB::drm_formula()}} object, or a plain
formula for a mean-only node. Plain formulas are wrapped with \code{bf()}.}

\item{family}{A \code{drmTMB} family (for example \code{\link[drmTMB:nbinom2]{drmTMB::nbinom2()}},
\code{\link[drmTMB:beta_binomial]{drmTMB::beta_binomial()}}, or \code{\link[stats:gaussian]{stats::gaussian()}}).}

\item{...}{Further arguments passed to \code{\link[drmTMB:drmTMB]{drmTMB::drmTMB()}} when the node is
fitted (for example \code{control}).}
}
\value{
A \code{drm_node} specification object.
}
\description{
\code{drm_node()} records the model for a single endogenous (response) node
without fitting it. It is the building block of the declarative interface
\code{\link[=drm_sem]{drm_sem()}}: each node becomes one \code{\link[drmTMB:drmTMB]{drmTMB::drmTMB()}} fit. The node's
distributional-parameter formulae (for example \code{mu}, \code{sigma}, \code{zi}, \code{hu},
\code{nu}, \code{sd(group)}, \code{rho12}) define \strong{component-labelled paths}: a predictor
in the \code{sigma} formula is a path to residual scale, not to the mean.
}
\examples{
if (requireNamespace("drmTMB", quietly = TRUE)) {
  abundance <- drm_node(
    drmTMB::bf(count ~ size + temp + (1 | site), sigma ~ temp, zi ~ habitat),
    family = drmTMB::nbinom2()
  )
  abundance
}
}
