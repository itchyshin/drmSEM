% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop.R
\name{as_dot}
\alias{as_dot}
\alias{as_dot.drm_sem}
\alias{as_dot.drm_dag}
\title{Export a distributional SEM as Graphviz DOT}
\usage{
as_dot(object, ...)

\method{as_dot}{drm_sem}(object, ...)

\method{as_dot}{drm_dag}(object, ...)
}
\arguments{
\item{object}{A \code{drm_sem} or a \code{drm_dag}.}

\item{...}{Unused.}
}
\value{
A length-1 character string of class \code{drm_dot} (its print method
\code{cat()}s the DOT source). Pipe it to Graphviz (\code{dot -Tpng}) or
\code{DiagrammeR::grViz()} to render.
}
\description{
\code{as_dot()} renders the \strong{component-labelled} structural graph as a Graphviz
DOT string: one edge per typed edge (\code{from -> to}), with the distributional
component (\code{mu}, \code{sigma}, \code{zi}, ...) as the edge label. Non-mean paths are
dashed and greyed so a \code{sigma} / \code{zi} arrow reads differently from a mean
arrow. Unlike \code{\link[=as_lavaan]{as_lavaan()}}, DOT keeps \strong{every} component path — nothing is
dropped.
}
\examples{
dag <- drm_dag(size ~ temp, abundance ~ size + temp)
as_dot(dag)
}
\seealso{
\code{\link[=as_lavaan]{as_lavaan()}}, \code{\link[=plot.drm_sem]{plot.drm_sem()}}.
}
