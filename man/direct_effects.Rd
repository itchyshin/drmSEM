% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{direct_effects}
\alias{direct_effects}
\title{Response-scale direct (controlled) effect of a predictor on a node}
\usage{
direct_effects(
  object,
  from,
  to,
  component = NULL,
  target = c("mean", "p_gt", "p_zero", "var", "quantile"),
  threshold = 0,
  prob = 0.5,
  functional = c("simulate", "analytic"),
  at = NULL,
  B = 200L,
  uncertainty = NULL,
  nsim = NULL,
  population = NULL,
  level = 0.95,
  seed = NULL,
  draw = NULL,
  n_sim = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{drm_sem} object.}

\item{from}{Predictor variable or node name.}

\item{to}{Endogenous target node.}

\item{component}{Optional component filter for the attached coefficient table.}

\item{target}{Functional of the outcome distribution to report the effect on:
\code{"mean"} (default), \code{"p_gt"} (Pr(Y > \code{threshold})), \code{"p_zero"} (Pr(Y = 0)),
\code{"var"} (Var(Y)), or \code{"quantile"} (the \code{prob}-quantile). Non-mean targets
simulate the outcome from its family (OQ-11) — most useful when a path moves
only \code{sigma}/\code{zi}/\code{nu}, which can shift a tail probability or quantile while
leaving the mean nearly unchanged.}

\item{threshold}{Cutoff for \code{target = "p_gt"}.}

\item{prob}{Probability for \code{target = "quantile"} (default \code{0.5}, the median).}

\item{functional}{How a non-mean \code{target} is evaluated: \code{"simulate"} (default;
draw the outcome from its family and summarize) or \code{"analytic"} (a
closed-form functional of the predicted parameters — no Monte-Carlo noise).
Analytic forms are offered for the gaussian and poisson families (others
abort with a pointer to \code{"simulate"}), and require mean mediation
(\code{method = "gcomp"}) so the outcome parameters are deterministic.}

\item{at}{Optional length-2 contrast values for \code{from}.}

\item{B}{Number of uncertainty replicates (coefficient draws) used when
\code{uncertainty = "parametric"}.}

\item{uncertainty}{How to propagate parameter uncertainty: \code{"parametric"}
(draw coefficients from \code{MVN(coef, vcov)}, the default), \code{"none"} (MLE point
estimate, no interval), or \code{"bootstrap"} (refit per replicate; not yet
implemented, OQ-10).}

\item{nsim}{Inner distributional realizations per uncertainty draw (used for
non-mean \code{target}s).}

\item{population}{\code{"conditional"} (random effects held at zero, the default)
or \code{"marginal"} (integrate over the fitted random-effect distribution; not
yet implemented, OQ-9).}

\item{level}{Confidence level for the Monte-Carlo interval.}

\item{seed}{Optional RNG seed.}

\item{draw, n_sim}{Deprecated aliases for \code{uncertainty} (\code{draw = TRUE}/\code{FALSE}
maps to \code{"parametric"}/\code{"none"}) and \code{nsim}; supplying either emits a
deprecation warning.}

\item{...}{Unused.}
}
\value{
A one-row data frame (\code{from}, \code{to}, \code{scale}, \code{target}, \code{estimate},
\code{conf.low}, \code{conf.high}) with a \code{coefficients} attribute.
}
\description{
The controlled direct effect holds all mediators at their observed values and
changes only \code{from}, so only the arrow(s) from \code{from} directly into \code{to}
operate. Reported as the population-average change in the chosen \code{target}
functional of \code{to} for a one-SD (numeric) or first-to-second-level (factor)
change in \code{from}. The fitted direct coefficients are attached as a
\code{coefficients} attribute.
}
\examples{
\dontrun{
sem <- drm_sem(
  size = drm_node(drmTMB::bf(size ~ temp + habitat, sigma ~ temp),
                  family = stats::gaussian()),
  abundance = drm_node(drmTMB::bf(abundance ~ size + temp, zi ~ habitat),
                       family = drmTMB::nbinom2()),
  data = dat)
# Controlled direct effect of temp on abundance, with parametric uncertainty.
direct_effects(sem, from = "temp", to = "abundance",
               uncertainty = "parametric", nsim = 50)
}
}
\references{
\insertRef{Robins1992}{drmSEM}

\insertRef{Pearl2001}{drmSEM}

\insertRef{Pearl2009}{drmSEM}

\insertRef{VanderWeele2015}{drmSEM}
}
