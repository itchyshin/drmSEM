% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputation.R
\name{imputed}
\alias{imputed}
\alias{imputed.drm_sem}
\title{Fitted missing-predictor values from a drmSEM graph}
\usage{
imputed(object, ...)

\method{imputed}{drm_sem}(
  object,
  variable = NULL,
  node = NULL,
  rows = c("missing", "all"),
  se = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{drm_sem} object.}

\item{...}{Unused.}

\item{variable}{Optional parent name. When omitted, every imputed
parent is stacked.}

\item{node}{Optional endogenous node name that restricts the stack.}

\item{rows}{\code{"missing"} or \code{"all"}, forwarded to the engine.}

\item{se}{Logical; forwarded to the engine.}
}
\value{
A data frame with \code{node} plus the engine columns \code{variable},
\code{original_row}, \code{model_row}, \code{observed}, \code{estimate}, \code{std_error},
\code{source}, and \code{uncertainty_status}. Zero rows when nothing was
imputed.
}
\description{
Walks every node that \code{\link[=drm_sem]{drm_sem()}} derived an imputation model for and
returns the engine's missing-predictor table, stacked, with a \code{node}
column. Omitting \code{variable} stacks every imputed parent — it never
silently returns the first \code{mi()} term only. Branch on
\code{uncertainty_status}, never on \code{is.na(std_error)}: observed rows and
\code{se = FALSE} requests report \code{std_error = NA} with status \code{"ok"}.
This is not multiple imputation, not Rubin's rules, and not
full-information maximum likelihood across the SEM.
}
\examples{
\dontrun{
sem <- drm_sem(
  m = drm_node(drmTMB::bf(m ~ x)),
  y = drm_node(drmTMB::bf(y ~ m + x)),
  data = dat, impute = "auto"
)
imputed(sem)
imputed(sem, variable = "m", rows = "all")
}
}
\seealso{
\code{\link[=imputation]{imputation()}}, \code{\link[=drm_sem]{drm_sem()}}.
}
