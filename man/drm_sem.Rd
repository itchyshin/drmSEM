% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drm_sem.R
\name{drm_sem}
\alias{drm_sem}
\title{Fit and assemble a distributional piecewise SEM}
\usage{
drm_sem(
  ...,
  data,
  covariances = NULL,
  composites = NULL,
  feedback = NULL,
  latent = NULL,
  na_action = c("warn", "common", "fail"),
  impute = c("none", "auto")
)
}
\arguments{
\item{...}{Named \code{\link[=drm_node]{drm_node()}} specifications, one per endogenous node.}

\item{data}{A data frame supplied to every node fit.}

\item{covariances}{Optional \code{\link[=covary]{covary()}} declaration(s) (one object or a list)
recording residual (\code{rho12}) or higher-level (\code{corpair}) covariance edges
between responses; see \code{\link[=covariances]{covariances()}}.}

\item{composites}{Optional \code{\link[=drm_composite]{drm_composite()}} declaration(s). Each construct
column is materialized from its indicators \emph{before} fitting, so node
formulas can reference it as an ordinary observed column; the loadings are
reported by \code{\link[=loadings]{loadings()}}.}

\item{feedback}{Optional \code{\link[=drm_cycle]{drm_cycle()}} declaration(s) naming a feedback
(cyclic) motif, allowing those nodes to form a cycle (every undeclared cycle
stays an error). Node-wise ML of a declared cycle is inconsistent under
simultaneity (a warning is emitted); equilibrium effects use the fixed-point
propagator. See \code{docs/design/10-cyclic-feedback.md}.}

\item{latent}{Character vector of \strong{marginalised} latent vertex names (\code{L_M}).
When supplied, \code{\link[=basis_set]{basis_set()}} and \code{\link[=dsep]{dsep()}} generate and test \strong{m-separation}
claims on the implied MAG, conditioning on Richardson & Spirtes (2002)
Corollary 5.3 anteriors (\verb{S = \\\\emptyset} only). Pairwise claims are jointly
licensed by Sadeghi & Lauritzen (2014) Theorem 3 / Lauritzen & Sadeghi (2018)
Theorem 4 under a compositional-graphoid assumption (automatic for
homoscedastic all-Gaussian nodes; otherwise faithfulness). Conditioned /
selection latents are not supported. See \code{docs/design/14-m-separation.md}.}

\item{na_action}{What to do when incomplete rows mean the nodes would not all
be fitted on the same rows. \code{"warn"} (default) fits anyway but reports which
nodes used how many rows; \code{"common"} fits every node on the shared
complete-case set, so the piecewise SEM describes one sample; \code{"fail"} makes
it an error. Each node's realized row count is recorded in
\code{attr(x, "alignment_issues")} and in \code{\link[=check_sem]{check_sem()}}.

A piecewise SEM whose nodes are fitted on different samples is not the model
the user asked for: path coefficients come from different row sets and the
d-separation basis set is tested against yet another. The default warns
rather than aborting so existing models keep fitting, but it never stays
silent.}

\item{impute}{Whether to derive missing-predictor imputation models from the
graph. \code{"none"} (default) leaves missing data to the row-alignment policy
above. \code{"auto"} derives, for each node with an incomplete \strong{endogenous}
parent, an imputation model equal to that parent's own node formula and
family, and passes it to the engine as \code{mi()} + \code{impute_model()}. The user
never writes an \code{impute_model()}: the causal graph specifies it, so the
conditioning set is derived rather than guessed, and imputer and analysis
model come from one graph.

Opt-in by design: imputation asserts a missing-at-random assumption, which
is the user's call to make, not a silent default.

Because the SEM is piecewise, the downstream node \strong{re-estimates} the
parent's model inside its own likelihood rather than sharing the parent
node's estimates. Imputation uncertainty is propagated within a node but
\strong{not across nodes}; this is a principled imputation, not full-information
maximum likelihood across the SEM. Inspect what was derived with
\code{\link[=imputation]{imputation()}}.}
}
\value{
A \code{drm_sem} object.
}
\description{
\code{drm_sem()} is the declarative interface. You describe each endogenous node
with \code{\link[=drm_node]{drm_node()}}; \code{drm_sem()} fits each node with \code{\link[drmTMB:drmTMB]{drmTMB::drmTMB()}} and then
builds the same object \code{\link[=drm_psem]{drm_psem()}} returns. Causal paths are
component-labelled: a predictor may target \code{mu}, \code{sigma}, \code{nu}, \code{zi}, \code{hu},
\code{sd(group)}, or \code{rho12} of a node.
}
\examples{
\dontrun{
sem <- drm_sem(
  size = drm_node(
    drmTMB::bf(size ~ temp + habitat + (1 | species), sigma ~ temp),
    family = stats::gaussian()
  ),
  abundance = drm_node(
    drmTMB::bf(abundance ~ size + temp + (1 | site), sigma ~ temp, zi ~ habitat),
    family = drmTMB::nbinom2()
  ),
  data = dat
)
paths(sem)
dsep(sem)
indirect_effects(sem, from = "temp", to = "abundance")
}
}
\references{
\insertRef{Shipley2009}{drmSEM}

\insertRef{Lefcheck2016}{drmSEM}

\insertRef{Brooks2017}{drmSEM}

\insertRef{Rigby2005}{drmSEM}
}
\seealso{
\code{\link[=drm_psem]{drm_psem()}}, \code{\link[=paths]{paths()}}, \code{\link[=dsep]{dsep()}}, \code{\link[=indirect_effects]{indirect_effects()}}.
}
