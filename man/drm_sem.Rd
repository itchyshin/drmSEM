% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drm_sem.R
\name{drm_sem}
\alias{drm_sem}
\title{Fit and assemble a distributional piecewise SEM}
\usage{
drm_sem(..., data, covariances = NULL, composites = NULL, feedback = NULL)
}
\arguments{
\item{...}{Named \code{\link[=drm_node]{drm_node()}} specifications, one per endogenous node.}

\item{data}{A data frame supplied to every node fit.}

\item{covariances}{Optional \code{\link[=covary]{covary()}} declaration(s) (one object or a list)
recording residual (\code{rho12}) or higher-level (\code{corpair}) covariance edges
between responses; see \code{\link[=covariances]{covariances()}}.}

\item{composites}{Optional \code{\link[=drm_composite]{drm_composite()}} declaration(s). Each construct
column is materialized from its indicators \emph{before} fitting, so node
formulas can reference it as an ordinary observed column; the loadings are
reported by \code{\link[=loadings]{loadings()}}.}

\item{feedback}{Optional \code{\link[=drm_cycle]{drm_cycle()}} declaration(s) naming a feedback
(cyclic) motif, allowing those nodes to form a cycle (every undeclared cycle
stays an error). Node-wise ML of a declared cycle is inconsistent under
simultaneity (a warning is emitted); equilibrium effects use the fixed-point
propagator. See \code{docs/design/10-cyclic-feedback.md}.}
}
\value{
A \code{drm_sem} object.
}
\description{
\code{drm_sem()} is the declarative interface. You describe each endogenous node
with \code{\link[=drm_node]{drm_node()}}; \code{drm_sem()} fits each node with \code{\link[drmTMB:drmTMB]{drmTMB::drmTMB()}} and then
builds the same object \code{\link[=drm_psem]{drm_psem()}} returns. Causal paths are
component-labelled: a predictor may target \code{mu}, \code{sigma}, \code{nu}, \code{zi}, \code{hu},
\code{sd(group)}, or \code{rho12} of a node.
}
\examples{
\dontrun{
sem <- drm_sem(
  size = drm_node(
    drmTMB::bf(size ~ temp + habitat + (1 | species), sigma ~ temp),
    family = stats::gaussian()
  ),
  abundance = drm_node(
    drmTMB::bf(abundance ~ size + temp + (1 | site), sigma ~ temp, zi ~ habitat),
    family = drmTMB::nbinom2()
  ),
  data = dat
)
paths(sem)
dsep(sem)
indirect_effects(sem, from = "temp", to = "abundance")
}
}
\references{
\insertRef{Shipley2009}{drmSEM}

\insertRef{Lefcheck2016}{drmSEM}

\insertRef{Brooks2017}{drmSEM}

\insertRef{Rigby2005}{drmSEM}
}
\seealso{
\code{\link[=drm_psem]{drm_psem()}}, \code{\link[=paths]{paths()}}, \code{\link[=dsep]{dsep()}}, \code{\link[=indirect_effects]{indirect_effects()}}.
}
