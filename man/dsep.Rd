% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsep.R
\name{dsep}
\alias{dsep}
\alias{dsep.drm_sem}
\title{Test directed-separation claims by likelihood-ratio refits}
\usage{
dsep(object, ...)

\method{dsep}{drm_sem}(object, ...)
}
\arguments{
\item{object}{A \code{drm_sem} object.}

\item{...}{Unused.}
}
\value{
A data frame of claims with \code{df}, \code{LR}, and \code{p.value}, carrying a
\code{fisher_c} attribute (see \code{\link[=fisher_c]{fisher_c()}}). A \code{status} column records why a
claim was not tested: \code{"no_data_column"} (the claim's variable is not a
column), \code{"refit_failed"}, \code{"n_mismatch"} (the augmented refit used a
different number of observations than the base fit, usually because the
added variable has missing values, so the likelihood ratio would compare two
different samples), or \code{"degenerate"} (non-nested or non-finite). Only
\code{"ok"} claims enter Fisher's C.

Two further columns report the claim's \strong{scale}. \code{n_effective} and
\code{scale_group} are \code{NA} when the claim's variable varies row by row. When it is
constant within a grouping the node does not already model -- a species-level
trait repeated down to individuals, say -- they name that grouping and its
number of levels, and \code{dsep()} warns.

That situation matters because the likelihood ratio treats every row as
independent evidence: a chance group-level association is credited with far
more support than it has, so the test \strong{rejects TRUE independences} rather
than missing false ones, and Fisher's C inherits the false rejection. The
remedy is to add the grouping term to that node so the base and augmented fits
share it. drmSEM reports rather than corrects, deliberately: adding the term to
only the augmented fit would compare two different random-effect structures,
which is not a valid likelihood-ratio test.
}
\description{
For each claim X \emph{||} Y in \code{\link[=basis_set]{basis_set()}}, \code{dsep()} refits node Y with X added
as a fixed-effect predictor to \strong{every modelled distributional component},
and compares it to the base node fit by a likelihood-ratio test. A small
p-value means X carries information about some component of Y beyond Y's
parents, i.e. a missing arrow.
}
\details{
This is the local-likelihood d-separation test of Shipley (2000, 2009),
extended in \code{drmSEM} from a mean-only test to an \strong{any-component} test (the
claim that X is irrelevant to \emph{every} modelled distributional parameter of
Y). This any-component extension is a \code{drmSEM} construction; it has been
calibrated against the family-grid data-generating processes in
\verb{inst/calibration/}.

Requires nodes fitted so that refits converge (the declarative \code{\link[=drm_sem]{drm_sem()}}
requests standard errors automatically).
}
\examples{
\dontrun{
sem <- drm_sem(
  size = drm_node(drmTMB::bf(size ~ temp + habitat, sigma ~ temp),
                  family = stats::gaussian()),
  abundance = drm_node(drmTMB::bf(abundance ~ size + temp, zi ~ habitat),
                       family = drmTMB::nbinom2()),
  data = dat)
dsep(sem)
}
}
\references{
\insertRef{Shipley2000}{drmSEM}

\insertRef{Shipley2009}{drmSEM}

\insertRef{Lefcheck2016}{drmSEM}

\insertRef{Pearl2009}{drmSEM}
}
