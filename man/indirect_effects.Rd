% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{indirect_effects}
\alias{indirect_effects}
\title{Indirect effect of a predictor on a node, with a distributional decomposition}
\usage{
indirect_effects(
  object,
  from,
  to,
  through = NULL,
  effect = c("controlled", "natural"),
  at = NULL,
  B = 200L,
  uncertainty = NULL,
  nsim = NULL,
  population = NULL,
  level = 0.95,
  seed = NULL,
  draw = NULL,
  n_sim = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{drm_sem} object.}

\item{from}{Predictor variable or node name.}

\item{to}{Endogenous target node.}

\item{through}{Optional set of mediator node names to route through. Defaults
to all mediators between \code{from} and \code{to}.}

\item{effect}{\code{"controlled"} (default) decomposes the total into a controlled
direct effect (mediators at observed values) plus mean-mediated and
distribution-mediated parts. \code{"natural"} reports the cross-world natural
direct/indirect effects (Pearl/Imai), holding the mediators at their
predicted \code{M(x0)} / \code{M(x1)} distributions; see
\code{docs/design/02-effect-calculus.md}.}

\item{at}{Optional length-2 contrast values for \code{from}.}

\item{B}{Number of uncertainty replicates (coefficient draws) used when
\code{uncertainty = "parametric"}.}

\item{uncertainty}{How to propagate parameter uncertainty: \code{"parametric"}
(draw coefficients from \code{MVN(coef, vcov)}, the default), \code{"none"} (MLE point
estimate, no interval), or \code{"bootstrap"} (refit per replicate; not yet
implemented, OQ-10).}

\item{nsim}{Inner distributional realizations per uncertainty draw (used for
non-mean \code{target}s).}

\item{population}{\code{"conditional"} (random effects held at zero, the default)
or \code{"marginal"} (integrate over the fitted random-effect distribution; not
yet implemented, OQ-9).}

\item{level}{Confidence level for the Monte-Carlo interval.}

\item{seed}{Optional RNG seed.}

\item{draw, n_sim}{Deprecated aliases for \code{uncertainty} (\code{draw = TRUE}/\code{FALSE}
maps to \code{"parametric"}/\code{"none"}) and \code{nsim}; supplying either emits a
deprecation warning.}

\item{...}{Unused.}
}
\value{
A \code{drm_effect} data frame. For \code{effect = "controlled"}, rows
\code{total_path}, \code{direct}, \code{indirect}, \code{mean_mediated}, \code{distribution_mediated}.
For \code{effect = "natural"}, rows \code{total_path}, \code{natural_direct},
\code{natural_indirect}, \code{mediated_interaction}.
}
\description{
The indirect effect is the simulation-based total path effect (mediators in
\code{through} allowed to respond) minus the controlled direct effect. It is
decomposed into a \strong{mean-mediated} part (mediator means propagate) and a
\strong{distribution-mediated} part (the extra effect that appears when mediators
pass realized draws, i.e. flowing through mediator scale / zero-inflation /
shape).
}
\details{
\code{indirect_effects()} does not take a \code{method} argument: the controlled
decomposition is formed from \emph{both} the mean-mediated and
distribution-mediated legs, and the natural decomposition always uses
distribution mediation, so neither has a single mean/distribution choice to
make. The shared \code{uncertainty}, \code{nsim}, and \code{population} controls apply as
elsewhere.
}
