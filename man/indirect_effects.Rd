% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{indirect_effects}
\alias{indirect_effects}
\title{Indirect effect of a predictor on a node, with a distributional decomposition}
\usage{
indirect_effects(
  object,
  from,
  to,
  through = NULL,
  effect = c("controlled", "natural"),
  target = c("mean", "p_gt", "p_zero", "var", "quantile"),
  threshold = 0,
  prob = 0.5,
  at = NULL,
  B = 200L,
  uncertainty = NULL,
  nsim = NULL,
  population = NULL,
  level = 0.95,
  seed = NULL,
  draw = NULL,
  n_sim = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{drm_sem} object.}

\item{from}{Predictor variable or node name.}

\item{to}{Endogenous target node.}

\item{through}{Optional set of mediator node names to route through. Defaults
to all mediators between \code{from} and \code{to}.}

\item{effect}{\code{"controlled"} (default) decomposes the total into a controlled
direct effect (mediators at observed values) plus mean-mediated and
distribution-mediated parts. \code{"natural"} reports the cross-world natural
direct/indirect effects (Pearl/Imai), holding the mediators at their
predicted \code{M(x0)} / \code{M(x1)} distributions; see
\code{docs/design/02-effect-calculus.md}.}

\item{target}{Functional of the outcome distribution to report the effect on:
\code{"mean"} (default), \code{"p_gt"} (Pr(Y > \code{threshold})), \code{"p_zero"} (Pr(Y = 0)),
\code{"var"} (Var(Y)), or \code{"quantile"} (the \code{prob}-quantile). Distributional
targets simulate the outcome from its family (OQ-11); see
\code{docs/design/02-effect-calculus.md}. For a feedback SEM only \code{"mean"} (the
equilibrium response) is defined.}

\item{threshold}{Cutoff for \code{target = "p_gt"}.}

\item{prob}{Probability for \code{target = "quantile"} (default \code{0.5}, the median).}

\item{at}{Optional length-2 contrast values for \code{from}.}

\item{B}{Number of uncertainty replicates (coefficient draws) used when
\code{uncertainty = "parametric"}.}

\item{uncertainty}{How to propagate parameter uncertainty: \code{"parametric"}
(draw coefficients from \code{MVN(coef, vcov)}, the default), \code{"none"} (MLE point
estimate, no interval), or \code{"bootstrap"} (refit per replicate; not yet
implemented, OQ-10).}

\item{nsim}{Inner distributional realizations per uncertainty draw (used for
non-mean \code{target}s).}

\item{population}{\code{"conditional"} (random effects held at zero, the default)
or \code{"marginal"} (integrate over the fitted random-effect distribution; not
yet implemented, OQ-9).}

\item{level}{Confidence level for the Monte-Carlo interval.}

\item{seed}{Optional RNG seed.}

\item{draw, n_sim}{Deprecated aliases for \code{uncertainty} (\code{draw = TRUE}/\code{FALSE}
maps to \code{"parametric"}/\code{"none"}) and \code{nsim}; supplying either emits a
deprecation warning.}

\item{...}{Unused.}
}
\value{
A \code{drm_effect} data frame. For \code{effect = "controlled"}, rows
\code{total_path}, \code{direct}, \code{indirect}, \code{mean_mediated}, \code{distribution_mediated}
and a \code{target} column naming the reported functional. For
\code{effect = "natural"}, rows \code{total_path}, \code{natural_direct},
\code{natural_indirect}, \code{mediated_interaction}.
}
\description{
The indirect effect is the simulation-based total path effect (mediators in
\code{through} allowed to respond) minus the controlled direct effect. It is
decomposed into a \strong{mean-mediated} part (mediator means propagate) and a
\strong{distribution-mediated} part (the extra effect that appears when mediators
pass realized draws, i.e. flowing through mediator scale / zero-inflation /
shape).
}
\details{
The controlled and natural direct/indirect estimands and their
identification under the counterfactual framework are due to Pearl (2001),
Robins & Greenland (1992), Imai et al. (2010), and VanderWeele (2015); the
four-way decomposition under exposure–mediator interaction is VanderWeele
(2014); the interventional estimand for multi-mediator settings is
Vansteelandt & Daniel (2017). The \strong{distribution-mediated} row reported
here is a \code{drmSEM} construction: it isolates the Jensen-gap part of an
indirect effect that flows through a mediator's higher-moment components
(\code{sigma}, \code{zi}, \code{nu}), with no coefficient-product analogue. The
construction sits on top of the cited mediation framework — it does not
replace any of these estimands.

\code{indirect_effects()} does not take a \code{method} argument: the controlled
decomposition is formed from \emph{both} the mean-mediated and
distribution-mediated legs, and the natural decomposition always uses
distribution mediation, so neither has a single mean/distribution choice to
make. The shared \code{uncertainty}, \code{nsim}, and \code{population} controls apply as
elsewhere.

A non-mean \code{target} (OQ-11) is supported for \code{effect = "controlled"}: every
leg then reports the contrast on that functional of the outcome (e.g. the
indirect change in \code{Pr(Y = 0)} or a tail quantile routed through the
mediators), and the mean-/distribution-mediated split still closes
(\code{indirect = mean_mediated + distribution_mediated}). \code{effect = "natural"}
supports only \code{target = "mean"} (the cross-world functional contrast is open,
OQ-8/OQ-11).
}
\examples{
\dontrun{
sem <- drm_sem(
  size = drm_node(drmTMB::bf(size ~ temp + habitat, sigma ~ temp),
                  family = stats::gaussian()),
  abundance = drm_node(drmTMB::bf(abundance ~ size + temp, zi ~ habitat),
                       family = drmTMB::nbinom2()),
  data = dat)
# Indirect effect of temp on abundance routed through size.
indirect_effects(sem, from = "temp", to = "abundance", through = "size",
                 uncertainty = "parametric", nsim = 50)
}
}
\references{
\insertRef{Robins1992}{drmSEM}

\insertRef{Pearl2001}{drmSEM}

\insertRef{Pearl2009}{drmSEM}

\insertRef{Imai2010}{drmSEM}

\insertRef{VanderWeele2014}{drmSEM}

\insertRef{VanderWeele2015}{drmSEM}

\insertRef{Vansteelandt2017}{drmSEM}
}
