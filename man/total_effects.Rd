% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{total_effects}
\alias{total_effects}
\title{Total effect of a predictor on a node by simulation}
\usage{
total_effects(
  object,
  from,
  to,
  method = NULL,
  target = c("mean", "p_gt", "p_zero", "var", "quantile"),
  threshold = 0,
  prob = 0.5,
  at = NULL,
  B = 200L,
  uncertainty = NULL,
  nsim = NULL,
  population = NULL,
  level = 0.95,
  seed = NULL,
  mediation = NULL,
  draw = NULL,
  n_sim = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{drm_sem} object.}

\item{from}{Predictor variable or node name.}

\item{to}{Endogenous target node.}

\item{method}{\code{"gcomp"} (mean mediation: deterministic g-computation on
mediator expectations, the default) or \code{"simulate"} (mediators pass realized
draws from their fitted families, capturing distribution-mediated paths).}

\item{target}{Functional of the outcome distribution to report the effect on:
\code{"mean"} (default), \code{"p_gt"} (Pr(Y > \code{threshold})), \code{"p_zero"} (Pr(Y = 0)),
\code{"var"} (Var(Y)), or \code{"quantile"} (the \code{prob}-quantile). Distributional
targets simulate the outcome from its family (OQ-11); see
\code{docs/design/02-effect-calculus.md}. For a feedback SEM only \code{"mean"} (the
equilibrium response) is defined.}

\item{threshold}{Cutoff for \code{target = "p_gt"}.}

\item{prob}{Probability for \code{target = "quantile"} (default \code{0.5}, the median).}

\item{at}{Optional length-2 contrast values for \code{from}.}

\item{B}{Number of uncertainty replicates (coefficient draws) used when
\code{uncertainty = "parametric"}.}

\item{uncertainty}{How to propagate parameter uncertainty: \code{"parametric"}
(draw coefficients from \code{MVN(coef, vcov)}, the default), \code{"none"} (MLE point
estimate, no interval), or \code{"bootstrap"} (refit per replicate; not yet
implemented, OQ-10).}

\item{nsim}{Inner distributional realizations per uncertainty draw (used for
non-mean \code{target}s).}

\item{population}{\code{"conditional"} (random effects held at zero, the default)
or \code{"marginal"} (integrate over the fitted random-effect distribution; not
yet implemented, OQ-9).}

\item{level}{Confidence level for the Monte-Carlo interval.}

\item{seed}{Optional RNG seed.}

\item{mediation}{Deprecated alias for \code{method} (\code{"mean"} maps to \code{"gcomp"},
\code{"distribution"} to \code{"simulate"}); supplying it emits a deprecation warning.}

\item{draw, n_sim}{Deprecated aliases for \code{uncertainty} (\code{draw = TRUE}/\code{FALSE}
maps to \code{"parametric"}/\code{"none"}) and \code{nsim}; supplying either emits a
deprecation warning.}

\item{...}{Unused.}
}
\value{
A one-row \code{drm_effect} data frame.
}
\description{
Propagates a do()-style change in \code{from} through the whole DAG (all mediators
respond) and reports the population-average change in the chosen \code{target}
functional of \code{to}. With \code{method = "simulate"}, mediators pass realized draws
from their families, so effects flowing through a mediator's scale,
zero-inflation, or shape (distribution-mediated paths) are included; with
\code{method = "gcomp"} only the mediator means propagate.
}
\details{
\strong{Feedback SEMs.} When the model declares a feedback motif (see \code{\link[=drm_cycle]{drm_cycle()}}),
the total effect is the system's \strong{equilibrium} response, computed by
iterating the mean-propagation map to its fixed point rather than by a single
topological sweep. Only \code{target = "mean"} is supported (the equilibrium is on
the deterministic mean map), the \code{mediation} column reads \code{"equilibrium"}, and
if the feedback diverges (no stable equilibrium, spectral radius \verb{>= 1}) the
estimate is reported as \code{NA} with a warning — never a fabricated number. The
mean/distribution decomposition through a cycle is out of scope, so
\code{\link[=indirect_effects]{indirect_effects()}} / \code{\link[=path_effects]{path_effects()}} refuse a feedback SEM. See
\code{docs/design/10-cyclic-feedback.md}.
}
\examples{
\dontrun{
sem <- drm_sem(
  size = drm_node(drmTMB::bf(size ~ temp + habitat, sigma ~ temp),
                  family = stats::gaussian()),
  abundance = drm_node(drmTMB::bf(abundance ~ size + temp, zi ~ habitat),
                       family = drmTMB::nbinom2()),
  data = dat)
# Total effect of temp on abundance, mediators allowed to respond by simulation.
total_effects(sem, from = "temp", to = "abundance",
              method = "simulate", uncertainty = "parametric", nsim = 50)
}
}
