% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pair.R
\name{drm_expand_pair}
\alias{drm_expand_pair}
\title{Expand a bivariate pair onto the covariance-edge grammar}
\usage{
drm_expand_pair(pair)
}
\arguments{
\item{pair}{A \code{drm_pair} object.}
}
\value{
A list with \code{nodes} (named list of \code{drm_node}) and \code{covariances}
(list of \code{drm_covary}).
}
\description{
Bridges a \code{\link[=drm_pair]{drm_pair()}} declaration onto the shipped pieces: two \code{\link[=drm_node]{drm_node()}}
specifications (the marginal response sub-nodes) and the \code{\link[=covary]{covary()}} covariance
edges (residual \code{rho12} plus any higher-level \code{corpair}). This is the
documented hook point for the 0.4 engine lane — a joint bivariate \code{drmTMB} fit
replaces the two independent node fits, while the covariance edges and
accessors stay the same. Building the \code{\link[=drm_node]{drm_node()}} objects wraps each plain
formula with \code{\link[drmTMB:bf]{drmTMB::bf()}}, so this needs \code{drmTMB} available; the declaration
itself (\code{\link[=drm_pair]{drm_pair()}}) and the accessors do not.
}
\seealso{
\code{\link[=drm_pair]{drm_pair()}}, \code{\link[=drm_sem]{drm_sem()}}.
}
