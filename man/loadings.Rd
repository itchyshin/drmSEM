% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composite.R
\name{loadings}
\alias{loadings}
\alias{loadings.drm_sem}
\title{Indicator loadings of a SEM's composite constructs}
\usage{
loadings(object, ...)

\method{loadings}{drm_sem}(object, ...)
}
\arguments{
\item{object}{A \code{drm_sem} object.}

\item{...}{Unused.}
}
\value{
A \code{drm_loadings} data frame with columns \code{composite}, \code{indicator},
\code{loading}, \code{std_loading} (the indicator's correlation with the construct
score; \code{NA} when the data is unavailable), and \code{method}.
}
\description{
Returns the indicator-to-construct loadings for every composite declared with
\code{\link[=drm_composite]{drm_composite()}}, reported separately from the structural \code{\link[=paths]{paths()}} (a
composite's measurement structure is not a causal path). Empty if the SEM has
no composites.
}
\examples{
\dontrun{
sem <- drm_sem(
  fitness = drm_node(drmTMB::bf(fitness ~ body_size), family = stats::gaussian()),
  data = dat,
  composites = drm_composite("body_size", c("len", "mass"), data = dat))
loadings(sem)
}
}
\seealso{
\code{\link[=drm_composite]{drm_composite()}}, \code{\link[=paths]{paths()}}.
}
